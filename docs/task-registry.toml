version = 1
project = "Yoctui"
status_values = ["NOT_STARTED", "IN_PROGRESS", "BLOCKED", "DONE"]

# Statuses reflect the implementation and test evidence present on master.
# Broad tasks remain IN_PROGRESS until every behavior in their notes is verified.

[[task]]
id = "GOV-001"
milestone = "M0"
title = "Reconcile governance roadmap with current repository"
status = "DONE"
priority = 1
required = true
depends_on = []
verify = ["./scripts/verify-roadmap.sh", "cargo test -p yoctui-model -p yoctui-ui -p yoctui-app"]
notes = "Reconciled shell, focus, palette, themes, animation, Layers, Tasks, Images, and Settings evidence in this governance commit."

[[task]]
id = "GOV-002"
milestone = "M0"
title = "Keep one valid active task"
status = "DONE"
priority = 2
required = true
depends_on = []
verify = ["./scripts/verify-roadmap.sh"]
notes = "docs/current-task.md exists and identifies one task."

[[task]]
id = "ARCH-001"
milestone = "M0"
title = "Document component boundaries and state flow"
status = "DONE"
priority = 3
required = true
depends_on = []
verify = ["test -s docs/architecture.md"]
notes = "Expanded architecture contract."

[[task]]
id = "UI-SHELL-001"
milestone = "M2"
title = "Persistent Header Navigator Workspace Inspector Footer shell"
status = "DONE"
priority = 10
required = true
depends_on = ["GOV-001"]
verify = ["cargo test -p yoctui-ui persistent_shell_degrades_across_supported_terminal_widths"]
notes = "Persistent five-region shell and non-blocking build navigation implemented by fc1b1ae and 4db7369; TestBackend breakpoint coverage added by 88b4aa7."

[[task]]
id = "UI-RESP-001"
milestone = "M2"
title = "Responsive wide medium narrow and too-small layouts"
status = "DONE"
priority = 11
required = true
depends_on = ["UI-SHELL-001"]
verify = ["cargo test -p yoctui-ui responsive", "cargo test -p yoctui-app responsive", "cargo test -p yoctui-model responsive"]
notes = "Wide three-pane, medium Inspector overlay, narrow visible pane switcher, modal-safe focus cycling, resize preservation, all-screen boundary, and minimum-size dialog coverage are complete in this commit."

[[task]]
id = "FOCUS-001"
milestone = "M2"
title = "Shared focus router and modal focus trapping"
status = "DONE"
priority = 12
required = true
depends_on = ["UI-SHELL-001"]
verify = ["cargo test -p yoctui-model focus", "cargo test -p yoctui-app focus", "cargo test -p yoctui-ui dialog", "cargo test -p yoctui -- focus"]
notes = "Central focus synchronization records and restores the exact pane across nested modal transitions, rejects pane actions while trapped, orders dialog input ahead of layer-browser state, and has reducer/app/CLI/UI coverage in this commit."

[[task]]
id = "DIALOG-001"
milestone = "M2"
title = "Unified typed dialog stack"
status = "DONE"
priority = 13
required = true
depends_on = ["FOCUS-001"]
verify = ["cargo test -p yoctui-model dialog", "cargo test -p yoctui-app dialog", "cargo test -p yoctui-ui dialog", "cargo test -p yoctui -- dialog"]
notes = "One typed FIFO dialog queue now owns build, image, recipe, Devtool, BBMASK, editor, quit, and completion state. Explicit reducer transitions, invalid-action preservation, exact focus restoration, retained asynchronous completion, CLI routing, and narrow TestBackend coverage are complete in this commit."

[[task]]
id = "THEME-001"
milestone = "M2"
title = "Built-in semantic themes"
status = "DONE"
priority = 14
required = true
depends_on = ["UI-SHELL-001"]
verify = ["cargo test -p yoctui-ui theme"]
notes = "One complete semantic palette now covers all five themes, shell/background, borders, selection, disabled state, status, severity, progress, dialogs, notifications, and syntax. Monochrome/no-color attributes and TestBackend coverage are complete in this commit."

[[task]]
id = "SETTINGS-001"
milestone = "M2"
title = "Interactive settings editing and persistence"
status = "DONE"
priority = 15
required = true
depends_on = ["THEME-001", "DIALOG-001"]
verify = ["cargo test -p yoctui-model settings", "cargo test -p yoctui-app settings", "cargo test -p yoctui-ui settings", "cargo test -p yoctui -- settings"]
notes = "Typed theme, animation, reduced-motion, color, log-wrap, and log-follow rows now preview immediately and persist atomically in session.toml. CLI precedence, unrelated-field preservation, dirty retry, failure notices, input mapping, and narrow UI coverage are complete in this commit."

[[task]]
id = "ANIM-001"
milestone = "M2"
title = "Indeterminate task animation and reduced motion"
status = "DONE"
priority = 16
required = true
depends_on = ["UI-SHELL-001"]
verify = ["cargo test -p yoctui-model animation", "cargo test -p yoctui-ui animation"]
notes = "UI-tick animation now distinguishes fast/slow cadence, reduced motion uses a stable active label, unknown progress never fabricates 0%, and determinate/terminal rows never animate. Theme, no-color, narrow, reducer, and TestBackend coverage are complete in this commit."

[[task]]
id = "PALETTE-001"
milestone = "M2"
title = "Searchable contextual command palette"
status = "DONE"
priority = 17
required = true
depends_on = ["UI-SHELL-001"]
verify = ["cargo test -p yoctui-model command_palette", "cargo test -p yoctui-app command_palette", "cargo test -p yoctui-ui command_palette", "cargo test -p yoctui -- command_palette"]
notes = "A typed 14-command catalog now owns deterministic ordering, case-insensitive label/description/shortcut search, bounded selection, contextual availability, exact disabled explanations, and existing-action dispatch. Empty/disabled activation, focus, CLI input, themes, and narrow rendering are covered in this commit."

[[task]]
id = "TASKS-001"
milestone = "M2"
title = "Complete live Tasks workspace"
status = "DONE"
priority = 18
required = true
depends_on = ["UI-SHELL-001", "BB-002"]
verify = ["cargo test -p yoctui-model live_tasks", "cargo test -p yoctui-ui live_tasks", "cargo test -p yoctui-app live_tasks", "cargo test -p yoctui-bitbake live_tasks"]
notes = "Typed runqueue statistics drive honest overall and aggregate waiting progress; active, completed, failed, cancelled, and lost rows, all specified filters, bounded selection, contextual task inspection, responsive rendering, and typed input are covered. The live harness passed against BitBake 2.19.0 / Poky 6.0.99 with task_queued, task_started, and positive authoritative task totals."

[[task]]
id = "JOB-001"
milestone = "M2"
title = "Persistent background-job domain model"
status = "DONE"
priority = 20
required = true
depends_on = ["GOV-001"]
verify = ["cargo test -p yoctui-model background_job"]
notes = "Typed lifecycle, context, progress, bounded output/history, result/error, cancellation capability, and reducer coverage implemented in this commit."

[[task]]
id = "JOB-002"
milestone = "M2"
title = "Background-job effect execution and cancellation"
status = "DONE"
priority = 21
required = true
depends_on = ["JOB-001"]
verify = ["cargo test -p yoctui-app background_job", "cargo test -p yoctui-bitbake cancellation"]
notes = "BuildJobCoordinator maps typed backend events into persistent job lifecycle/output, preserves navigation, distinguishes cancellation/failure/loss, and has app plus fake-process cancellation coverage in this commit."

[[task]]
id = "BB-001"
milestone = "M1"
title = "Real BitBake smoke harness"
status = "DONE"
priority = 30
required = true
depends_on = ["JOB-002"]
verify = ["./scripts/verify-live-bitbake.sh"]
notes = "Production Tinfoil bridge and opt-in harness validated BitBake 2.19.0 / Poky 6.0.99 snapshot on qemux86-64, including workspace, metadata, native events, completion, cancellation, and shutdown; deterministic preflight tests are included in this commit."

[[task]]
id = "BB-002"
milestone = "M1"
title = "Typed backend-to-model event boundary"
status = "DONE"
priority = 31
required = true
depends_on = ["GOV-001"]
verify = ["./scripts/verify-ui-spec.sh", "cargo test -p yoctui-protocol typed_event", "cargo test -p yoctui-bitbake typed_event", "cargo test -p yoctui-app typed_event", "cargo test -p yoctui-model typed_event"]
notes = "Typed workspace and metadata payloads now flow protocol -> backend -> app normalization -> reducer. Initial and refresh discovery use reducer actions, missing progress remains unknown, future events are ignored safely, terminal events update primary and job state exactly once, and UI boundary enforcement rejects raw backend parsing."

[[task]]
id = "LOG-001"
milestone = "M1"
title = "Bounded searchable logs"
status = "DONE"
priority = 32
required = true
depends_on = ["BB-002"]
verify = ["cargo test -p yoctui-model log", "cargo test -p yoctui-ui log", "cargo test -p yoctui-app log"]
notes = "Bounded retention now prefers protected diagnostics, safely truncates oversized entries, coalesces repeated ordinary output, and exposes all pressure counters. Typed selection drives follow/pause, bounded scrolling, incremental search, next/previous matches, severity/recipe/task/build filters, full Inspector metadata, persisted build filter, source editing, and clipboard effects with responsive TestBackend coverage."

[[task]]
id = "ERR-001"
milestone = "M1"
title = "Structured error investigation"
status = "DONE"
priority = 33
required = true
depends_on = ["BB-002"]
verify = ["cargo test -p yoctui-model error", "cargo test -p yoctui-ui error", "cargo test -p yoctui-app error"]
notes = "Retained warnings/errors now gain stable IDs and typed categories, summaries, metadata, and suggestions. The complete list and contextual Inspector include build/recipe/task/source/related context; selection is bounded, source opening is typed, exact log jumps preserve filters, diagnostic loss is visible, and success/warning/failure/cancellation/backend-loss outcomes are distinct and actionable."

[[task]]
id = "LAYERS-001"
milestone = "M3"
title = "Lazy Layers tree and contextual inspector"
status = "DONE"
priority = 40
required = true
depends_on = ["JOB-002"]
verify = ["cargo test -p yoctui-model layer_tree", "cargo test -p yoctui-ui layer_tree", "cargo test -p yoctui-app layer_tree", "cargo test -p yoctui -- layer_tree"]
notes = "The pinned configured-layer inventory, stable-path cached lazy tree, subtree refresh, hidden/search filtering, Git decorations, active highlighting, typed Inspector modes, stale-response rejection, and 64 KiB binary-safe line-numbered previews are covered across reducer, input, CLI integration, and responsive TestBackend tests."

[[task]]
id = "RECIPES-001"
milestone = "M3"
title = "Complete recipes workspace actions"
status = "DONE"
priority = 43
required = true
depends_on = ["RECIPES-META-001", "RECIPES-UI-001", "RECIPES-ACTIONS-001"]
verify = ["cargo test -p yoctui-ui recipes", "cargo test -p yoctui-app recipe_action"]
notes = "Authoritative live-validated metadata, the identity-stable searchable Inspector, typed BitBake/navigation/Devtool/QA actions, responsive disabled reasons, and persistent outcomes now satisfy the complete Recipes workspace gate."

[[task]]
id = "RECIPES-META-001"
milestone = "M3"
title = "Add authoritative typed recipe metadata"
status = "DONE"
priority = 40
required = true
depends_on = ["BB-002"]
verify = ["cargo test -p yoctui-protocol recipe_metadata", "cargo test -p yoctui-bitbake recipe_metadata", "cargo test -p yoctui-model recipe_metadata", "python3 -m pytest bridge/tests -k recipe_metadata"]
notes = "Protocol v1 now defaults old summaries safely and carries resolved provider/version/append summaries plus lazy typed detail. Real BitBake 2.19.0/Poky 6.0.99 returned the base-files 3.0.14 provider, 39 tasks, one metadata source, and four packages; unavailable workspace/build/history fields remain None."

[[task]]
id = "RECIPES-UI-001"
milestone = "M3"
title = "Complete searchable Recipes Inspector"
status = "DONE"
priority = 41
required = true
depends_on = ["RECIPES-META-001"]
verify = ["cargo test -p yoctui-model recipes_workspace", "cargo test -p yoctui-ui recipes_workspace", "cargo test -p yoctui-app recipes_workspace"]
notes = "Absolute recipe identity now drives search, rendering, metadata loads, dependencies, and actions. Rows and Inspector expose all summary/detail fields, explicit load/error/empty/unavailable states, and typed task/build enrichment across responsive modes."

[[task]]
id = "RECIPES-ACTIONS-001"
milestone = "M3"
title = "Complete typed recipe actions"
status = "DONE"
priority = 43
required = true
depends_on = ["RECIPE-BITBAKE-001", "RECIPE-NAV-001", "RECIPE-QA-001"]
verify = ["cargo test -p yoctui-model recipe_", "cargo test -p yoctui-app recipe_", "cargo test -p yoctui-ui recipe_", "cargo test -p yoctui -- recipe_"]
notes = "Standard BitBake operations, authoritative provider/log/patch navigation, existing Devtool routes, and capability-aware CVE/SPDX background jobs are complete with reducer, app, CLI, and TestBackend coverage."

[[task]]
id = "RECIPE-BITBAKE-001"
milestone = "M3"
title = "Complete typed recipe BitBake operations"
status = "DONE"
priority = 40
required = true
depends_on = ["RECIPES-UI-001", "JOB-002"]
verify = ["cargo test -p yoctui-model recipe_bitbake_action", "cargo test -p yoctui-app recipe_bitbake_action", "cargo test -p yoctui-ui recipe_bitbake_action", "cargo test -p yoctui -- recipe_bitbake_action"]
notes = "Build, metadata-backed force-task selection, clean, cleansstate, devshell, menuconfig, diffconfig, and diffsigs now share validated typed requests, exact confirmation, backend target/task/force preservation, persistent jobs, and reducer/app/CLI/TestBackend coverage."

[[task]]
id = "RECIPE-NAV-001"
milestone = "M3"
title = "Complete recipe file, log, patch, and Devtool routes"
status = "DONE"
priority = 41
required = true
depends_on = ["RECIPE-BITBAKE-001"]
verify = ["cargo test -p yoctui-model recipe_navigation", "cargo test -p yoctui-app recipe_navigation", "cargo test -p yoctui-ui recipe_navigation", "cargo test -p yoctui -- recipe_navigation"]
notes = "Authoritative provider/editor, retained task-log and BitBake-resolved local patch pickers, stale/missing/editor failure reporting, responsive disabled explanations, and existing typed Devtool route integration are complete."

[[task]]
id = "RECIPE-QA-001"
milestone = "M3"
title = "Add recipe CVE and SPDX background actions"
status = "DONE"
priority = 42
required = true
depends_on = ["RECIPE-NAV-001", "JOB-002"]
verify = ["cargo test -p yoctui-model recipe_qa_action", "cargo test -p yoctui-app recipe_qa_action", "cargo test -p yoctui-ui recipe_qa_action", "cargo test -p yoctui -- recipe_qa_action"]
notes = "Authoritative do_cve_check/do_create_spdx capability gates, exact typed confirmation, distinct persistent CVE/SPDX jobs, retained output, honest absent artifacts, and success/failure/cancellation/backend-loss coverage are complete."

[[task]]
id = "CONFIG-001"
milestone = "M3"
title = "Configuration provenance workspace"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-META-001", "CONFIG-UI-001", "CONFIG-ACTIONS-001", "CONFIG-EDIT-001"]
verify = ["cargo test -p yoctui-protocol config_", "cargo test -p yoctui-bitbake config_", "cargo test -p yoctui-model config_", "cargo test -p yoctui-app config_", "cargo test -p yoctui-ui config_", "cargo test -p yoctui -- config_"]
notes = "Authoritative live-validated variable detail, responsive search/inspection, typed copy/source/scope/comparison, and allowlisted previewed atomic edits are complete."

[[task]]
id = "CONFIG-META-001"
milestone = "M3"
title = "Add authoritative typed variable detail"
status = "DONE"
priority = 42
required = true
depends_on = ["BB-001"]
verify = ["cargo test -p yoctui-protocol config_metadata", "cargo test -p yoctui-bitbake config_metadata", "cargo test -p yoctui-app config_metadata", "cargo test -p yoctui-model config_metadata", "python3 -m pytest bridge/tests -k config_metadata"]
notes = "Live bridge/Tinfoil validation passed against BitBake 2.19.0 in the qemux86-64 build: MACHINE expanded/unexpanded to qemux86-64, provenance was build/conf/toolcfg.conf:6, the typed set operation retained that file/line/value, and active overrides included linux, x86-64, qemuall, qemux86-64, poky, class-target, libc-glibc, and forcevariable. A live OVERRIDES query also preserved its unexpanded expression and bitbake.conf:803 operation. Focused and baseline tests pass."

[[task]]
id = "CONFIG-UI-001"
milestone = "M3"
title = "Complete searchable Configuration Inspector"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-META-001"]
verify = ["cargo test -p yoctui-model config_workspace", "cargo test -p yoctui-app config_workspace", "cargo test -p yoctui-ui config_workspace"]
notes = "Filtered identity-stable selection, typed Enter refresh, scoped stale-response isolation, loading/error/empty/populated states, full provenance/override/operation rendering, responsive layouts, and reducer/app/CLI/TestBackend coverage are complete."

[[task]]
id = "CONFIG-ACTIONS-001"
milestone = "M3"
title = "Add typed configuration copy, source, and compare actions"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-COPY-001", "CONFIG-SOURCE-001", "CONFIG-SCOPE-001", "CONFIG-COMPARE-001"]
verify = ["cargo test -p yoctui-model config_action", "cargo test -p yoctui-app config_action", "cargo test -p yoctui-ui config_action", "cargo test -p yoctui -- config_action"]
notes = "Typed effective/unexpanded copy, defining-source selection, recipe scope, and global-vs-recipe comparison children are complete with shared identity-safe availability."

[[task]]
id = "CONFIG-COPY-001"
milestone = "M3"
title = "Add typed configuration value copy actions"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-UI-001"]
verify = ["cargo test -p yoctui-model config_copy", "cargo test -p yoctui-app config_copy", "cargo test -p yoctui-ui config_copy", "cargo test -p yoctui -- config_copy"]
notes = "Typed C/U routes copy only loaded effective/unexpanded detail for the exact selected identity through the shared clipboard effect; partial states stay inert with shared responsive disabled reasons and reducer/app/CLI/TestBackend coverage."

[[task]]
id = "CONFIG-SOURCE-001"
milestone = "M3"
title = "Add authoritative configuration source selection"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-COPY-001"]
verify = ["cargo test -p yoctui-model config_source", "cargo test -p yoctui-app config_source", "cargo test -p yoctui-ui config_source", "cargo test -p yoctui -- config_source"]
notes = "The o route now uses exact loaded operation file/line identities, opens one source directly or a focus-trapping multi-source picker, bounds relative paths to the build directory, restores focus, and reports all partial/stale/editor states with reducer/app/CLI/TestBackend coverage."

[[task]]
id = "CONFIG-SCOPE-001"
milestone = "M3"
title = "Add recipe-scoped configuration inspection"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-SOURCE-001"]
verify = ["cargo test -p yoctui-model config_scope", "cargo test -p yoctui-app config_scope", "cargo test -p yoctui-ui config_scope", "cargo test -p yoctui -- config_scope"]
notes = "The s picker selects global or authoritative recipe inventory scope, emits exact typed queries, retains independent scoped lifecycle/detail, resets disappeared recipes to global, and makes copy/source follow the active identity with reducer/app/CLI/TestBackend coverage."

[[task]]
id = "CONFIG-COMPARE-001"
milestone = "M3"
title = "Compare typed configuration values"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-SCOPE-001"]
verify = ["cargo test -p yoctui-model config_compare", "cargo test -p yoctui-app config_compare", "cargo test -p yoctui-ui config_compare"]
notes = "The c dialog compares exact loaded global and active recipe identities with typed equal/different/unavailable effective and unexpanded outcomes, precise partial-state gating, focus restoration, and responsive tests."

[[task]]
id = "CONFIG-EDIT-001"
milestone = "M3"
title = "Add previewed configuration editing and refresh"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-EDIT-PREVIEW-001", "CONFIG-EDIT-WRITE-001"]
verify = ["cargo test -p yoctui-model config_edit", "cargo test -p yoctui-app config_edit", "cargo test -p yoctui-ui config_edit", "cargo test -p yoctui -- config_edit"]
notes = "The allowlisted global editor requires an exact preview, atomically updates local.conf, preserves unrelated content and permissions, and refreshes the exact typed identity."

[[task]]
id = "CONFIG-EDIT-PREVIEW-001"
milestone = "M3"
title = "Add allowlisted configuration edit preview"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-ACTIONS-001"]
verify = ["cargo test -p yoctui-model config_edit_preview", "cargo test -p yoctui-app config_edit_preview", "cargo test -p yoctui-ui config_edit_preview"]
notes = "MACHINE and DISTRO are the explicit global-only allowlist; E opens an authoritative prefilled editor, rejects control injection, and previews the exact escaped local.conf assignment before a typed write effect, with reducer/input/CLI/responsive TestBackend coverage."

[[task]]
id = "CONFIG-EDIT-WRITE-001"
milestone = "M3"
title = "Write and refresh previewed configuration edits"
status = "DONE"
priority = 42
required = true
depends_on = ["CONFIG-EDIT-PREVIEW-001"]
verify = ["cargo test -p yoctui-model config_edit_write", "cargo test -p yoctui-app config_edit_write", "cargo test -p yoctui -- config_edit_write"]
notes = "The CLI revalidates typed identity/value/destination/assignment, replaces or appends exact base-variable lines through a permission-preserving same-directory atomic rename, and refreshes only that identity. Synthetic CRLF/tamper/failure tests pass. A copied live BitBake 2.19.0 qemux86-64 local.conf passed the writer; a live bridge refresh returned MACHINE=qemux86-64 from conf/toolcfg.conf:6, and the live local.conf SHA-256 remained 94d5016d765dd1069e7f88a3f4428b2be145a21f2e2db8ccb58af3d26f0acd4c."

[[task]]
id = "DEVTOOL-001"
milestone = "M3"
title = "Complete Devtool lifecycle"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-META-001", "DEVTOOL-JOBS-001", "DEVTOOL-MODIFY-001", "DEVTOOL-PUBLISH-001", "DEVTOOL-TARGET-001"]
verify = ["cargo test -p yoctui-bitbake devtool_", "cargo test -p yoctui-model devtool_", "cargo test -p yoctui-app devtool_", "cargo test -p yoctui-ui devtool_", "cargo test -p yoctui -- devtool_"]
notes = "Authoritative status/Git state, persistent cancellable execution, modify/edit/build, update-recipe, configured-layer finish, deploy-target, and destructive reset all pass adapter, model, app, CLI, TestBackend, and baseline verification."

[[task]]
id = "DEVTOOL-META-001"
milestone = "M3"
title = "Add authoritative Devtool workspace and Git status"
status = "DONE"
priority = 43
required = true
depends_on = ["JOB-002"]
verify = ["cargo test -p yoctui-bitbake devtool_metadata", "cargo test -p yoctui-model devtool_metadata", "cargo test -p yoctui-app devtool_metadata", "cargo test -p yoctui-ui devtool_metadata"]
notes = "Typed absolute recipe identity, executable capability, workspace membership/path, Git porcelain-v2 branch/head/dirty/conflict state, shared action availability, fake-process/error coverage, responsive rendering, and live `devtool status` validation against the qemux86-64 build are complete."

[[task]]
id = "DEVTOOL-JOBS-001"
milestone = "M3"
title = "Run Devtool operations as persistent background jobs"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-JOB-SPEC-001", "DEVTOOL-JOB-RUNNER-001", "DEVTOOL-JOB-LIFECYCLE-001"]
verify = ["cargo test -p yoctui-bitbake devtool_job", "cargo test -p yoctui-model devtool_job", "cargo test -p yoctui-app devtool_job", "cargo test -p yoctui -- devtool_job"]
notes = "Typed shell-free command specs, the cancellable bounded-output process runner, and persistent reducer/CLI lifecycle integration are complete with independent BitBake coordination."

[[task]]
id = "DEVTOOL-JOB-SPEC-001"
milestone = "M3"
title = "Define typed Devtool operation commands"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-META-001", "JOB-002"]
verify = ["cargo test -p yoctui-model devtool_job_spec", "cargo test -p yoctui-bitbake devtool_job_spec"]
notes = "Model-owned validated modify/update-recipe/finish/deploy-target/undeploy-target/reset operations translate in yoctui-bitbake to exact shell-free OsString arguments, rejecting option/control ambiguity and relative finish destinations while preserving non-UTF-8 paths."

[[task]]
id = "DEVTOOL-JOB-RUNNER-001"
milestone = "M3"
title = "Add cancellable Devtool process streaming"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-JOB-SPEC-001"]
verify = ["cargo test -p yoctui-bitbake devtool_job_runner"]
notes = "The shell-free runner emits typed started/stdout/stderr/completed/failed/cancelled/lost events through a bounded channel, caps lines at 64 KiB, preserves invalid UTF-8 lossily, rejects duplicates, distinguishes start failures, and uses Unix process-group TERM/KILL cancellation with fake-process coverage."

[[task]]
id = "DEVTOOL-JOB-LIFECYCLE-001"
milestone = "M3"
title = "Integrate Devtool persistent job lifecycle"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-JOB-RUNNER-001"]
verify = ["cargo test -p yoctui-model devtool_job_lifecycle", "cargo test -p yoctui-app devtool_job_lifecycle", "cargo test -p yoctui -- devtool_job_lifecycle"]
notes = "Queue/run/output/terminal reducer integration, duplicate rejection, typed stdout/stderr retention, navigation persistence, cancellation routing, backend/process-loss handling, independent BitBake coordination, and TestBackend result rendering pass without terminal suspension."

[[task]]
id = "DEVTOOL-MODIFY-001"
milestone = "M3"
title = "Complete Devtool modify, edit, and build workflow"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-JOBS-001"]
verify = ["cargo test -p yoctui-model devtool_modify", "cargo test -p yoctui-app devtool_modify", "cargo test -p yoctui-ui devtool_modify", "cargo test -p yoctui -- devtool_modify"]
notes = "Exact-identity status gating, modify confirmation, persistent completion refresh, authoritative two-pane workspace editing, dirty-save protection, Ctrl+B typed recipe build confirmation, and recoverable refresh/source/editor failures are covered across model, app, CLI, and TestBackend tests."

[[task]]
id = "DEVTOOL-PUBLISH-001"
milestone = "M3"
title = "Complete Devtool update-recipe and finish workflows"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-UPDATE-001", "DEVTOOL-FINISH-001"]
verify = ["cargo test -p yoctui-model devtool_publish", "cargo test -p yoctui-app devtool_publish", "cargo test -p yoctui-ui devtool_publish", "cargo test -p yoctui -- devtool_publish"]
notes = "Update-recipe and finish children provide exact-identity eligibility, persistent success refresh, durable failure context, and configured-layer-only native finish destinations."

[[task]]
id = "DEVTOOL-UPDATE-001"
milestone = "M3"
title = "Complete Devtool update-recipe workflow"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-MODIFY-001"]
verify = ["cargo test -p yoctui-bitbake devtool_publish_update", "cargo test -p yoctui-model devtool_publish_update", "cargo test -p yoctui-app devtool_publish_update", "cargo test -p yoctui-ui devtool_publish_update", "cargo test -p yoctui -- devtool_publish_update"]
notes = "Exact-identity authoritative eligibility, provider-aware confirmation, shell-free arguments, persistent execution, original-identity success refresh after navigation, and nonzero/refresh failure retention pass adapter, model, app, CLI, and TestBackend coverage."

[[task]]
id = "DEVTOOL-FINISH-001"
milestone = "M3"
title = "Complete Devtool finish workflow"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-UPDATE-001"]
verify = ["cargo test -p yoctui-bitbake devtool_publish_finish", "cargo test -p yoctui-model devtool_publish_finish", "cargo test -p yoctui-app devtool_publish_finish", "cargo test -p yoctui-ui devtool_publish_finish", "cargo test -p yoctui -- devtool_publish_finish"]
notes = "Clean committed Git eligibility, absolute configured-layer picker, stale/unconfigured revalidation, provider/layer/path confirmation, native-byte adapter preservation, persistent success refresh, and failure context pass all focused and baseline checks."

[[task]]
id = "DEVTOOL-TARGET-001"
milestone = "M3"
title = "Complete Devtool deploy-target and reset workflows"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-DEPLOY-001", "DEVTOOL-RESET-001"]
verify = ["cargo test -p yoctui-model devtool_target", "cargo test -p yoctui-app devtool_target", "cargo test -p yoctui-ui devtool_target", "cargo test -p yoctui -- devtool_target"]
notes = "Exact-identity deploy-target and reset children validate and revalidate typed destructive/target intent, run persistently with durable outcomes, and refresh the original identity after success."

[[task]]
id = "DEVTOOL-DEPLOY-001"
milestone = "M3"
title = "Complete Devtool deploy-target workflow"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-PUBLISH-001"]
verify = ["cargo test -p yoctui-bitbake devtool_target_deploy", "cargo test -p yoctui-model devtool_target_deploy", "cargo test -p yoctui-app devtool_target_deploy", "cargo test -p yoctui-ui devtool_target_deploy", "cargo test -p yoctui -- devtool_target_deploy"]
notes = "Exact-identity workspace eligibility, identity-retaining target draft/plan, pre-preview and pre-execution token validation, provider-aware confirmation, shell-free arguments, persistent failure retention, and original-identity success refresh pass all checks."

[[task]]
id = "DEVTOOL-RESET-001"
milestone = "M3"
title = "Complete Devtool reset workflow"
status = "DONE"
priority = 43
required = true
depends_on = ["DEVTOOL-DEPLOY-001"]
verify = ["cargo test -p yoctui-bitbake devtool_target_reset", "cargo test -p yoctui-model devtool_target_reset", "cargo test -p yoctui-app devtool_target_reset", "cargo test -p yoctui-ui devtool_target_reset", "cargo test -p yoctui -- devtool_target_reset"]
notes = "Exact-identity removable-source eligibility, provider/source destructive confirmation, stale-source revalidation, shell-free arguments, persistent failure retention, and expected NotMember success refresh pass all focused and baseline checks."

[[task]]
id = "DEP-001"
milestone = "M3"
title = "Dependency exploration and why-built workflow"
status = "DONE"
priority = 44
required = true
depends_on = ["DEP-MODEL-001", "DEP-ADAPTER-001", "DEP-UI-001"]
verify = ["cargo test -p yoctui-bitbake dependency", "cargo test -p yoctui-ui dependency"]
notes = "Typed graph/path state, authoritative structured and bounded fallback acquisition, and the responsive graph-only workspace all pass focused, baseline, and live BitBake verification."

[[task]]
id = "DEP-MODEL-001"
milestone = "M3"
title = "Add typed dependency graph and why-built paths"
status = "DONE"
priority = 44
required = true
depends_on = ["BB-001"]
verify = ["cargo test -p yoctui-model dependency_graph", "cargo test -p yoctui-app dependency_graph"]
notes = "Typed recipe/task nodes and build/runtime/task edges now normalize deterministically with explicit truncation, reverse lookup, cycle-safe bounded shortest why-built paths, explicit load/empty/partial/failure states, identity-stable reducer selection, legacy-list compatibility, and typed app event mapping; focused and baseline verification pass."

[[task]]
id = "DEP-ADAPTER-001"
milestone = "M3"
title = "Acquire authoritative dependency graphs"
status = "DONE"
priority = 44
required = true
depends_on = ["DEP-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake dependency_graph", "python3 -m pytest bridge/tests -k dependenc"]
notes = "Additive protocol graph events, structured generateDepTreeEvent acquisition, legacy fallback, and bounded shell-free bitbake -g task-dot parsing cover malformed, oversized, nonzero, missing-tool/output, and symlink-escape failures. Live BitBake 2.19.0 / Yocto 6.0.99 snapshot returned 962 nodes and 1779 build/runtime/task edges; focused and baseline checks pass."

[[task]]
id = "DEP-UI-001"
milestone = "M3"
title = "Integrate dependency and why-built workspace"
status = "DONE"
priority = 44
required = true
depends_on = ["DEP-ADAPTER-001"]
verify = ["cargo test -p yoctui-model dependency_workspace", "cargo test -p yoctui-app dependency_workspace", "cargo test -p yoctui-ui dependency_workspace", "cargo test -p yoctui -- dependency_workspace"]
notes = "Graph-only typed rows, explicit load/empty/partial/failure rendering, reverse/outgoing Inspector context, bounded why-built paths, identity-stable selection, authoritative recipe/provider/log actions, app-owned input, and responsive TestBackend coverage pass all checks."

[[task]]
id = "SIG-001"
milestone = "M3"
title = "Signature dump and comparison workflows"
status = "DONE"
priority = 45
required = true
depends_on = ["SIG-MODEL-001", "SIG-ADAPTER-001", "SIG-UI-001"]
verify = ["cargo test -p yoctui-app signature"]
notes = "Typed signature state, shell-free authoritative adapters, responsive task-picker/workspace integration, background cancellation, provider navigation, and focused parent verification are complete."

[[task]]
id = "SIG-MODEL-001"
milestone = "M3"
title = "Add typed signature dump and comparison state"
status = "DONE"
priority = 45
required = true
depends_on = ["JOB-002"]
verify = ["cargo test -p yoctui-model signature_model", "cargo test -p yoctui-app signature_model"]
notes = "Exact recipe/task/hash/path identities, explicit bounded dump/comparison states, deterministic typed differences, identity-stable selection, stale-result correlation, reducer effects, and typed backend-event mapping pass focused and baseline checks."

[[task]]
id = "SIG-ADAPTER-001"
milestone = "M3"
title = "Acquire and compare authoritative BitBake signatures"
status = "DONE"
priority = 45
required = true
depends_on = ["SIG-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake signature_adapter", "cargo test -p yoctui-app signature_adapter"]
notes = "Shell-free bounded dumpsig/diffsigs adapters validate canonical artifact paths, exact recipe/task/hash correlation, timeout/cancellation, typed parsing and failures. Live BitBake 2.19.0 smoke found one autoconf-native do_fetch and one do_recipe_qa record, produced 113 typed differences, no dump limitations, and one explicit recursive-diffsigs limitation."

[[task]]
id = "SIG-UI-001"
milestone = "M3"
title = "Integrate signature dump and comparison workflows"
status = "DONE"
priority = 45
required = true
depends_on = ["SIG-ADAPTER-001"]
verify = ["cargo test -p yoctui-model signature_workspace", "cargo test -p yoctui-app signature_workspace", "cargo test -p yoctui-ui signature_workspace", "cargo test -p yoctui -- signature_workspace"]
notes = "Uppercase Z opens an authoritative focus-trapped task picker and a non-navigator child workspace with identity-stable records/sides, typed details/differences, responsive layouts, provider navigation, and cancellable background execution. Focused, parent, and baseline checks pass."

[[task]]
id = "PKG-001"
milestone = "M3"
title = "Package data browser"
status = "DONE"
priority = 46
required = true
depends_on = ["PKG-MODEL-001", "PKG-ADAPTER-001", "PKG-UI-001"]
verify = ["cargo test -p yoctui-app pkgdata"]
notes = "Exact bounded package state, authoritative shell-free acquisition, and the responsive Packages Navigator workspace pass all child, parent, and baseline checks. Live package-data compatibility remains unclaimed because the available build has no generated pkgdata."

[[task]]
id = "PKG-MODEL-001"
milestone = "M3"
title = "Add typed package data state"
status = "DONE"
priority = 46
required = true
depends_on = ["JOB-002"]
verify = ["cargo test -p yoctui-model pkgdata_model", "cargo test -p yoctui-app pkgdata_model"]
notes = "Exact package identities, available-versus-unavailable fields, explicit bounded inventory/detail states, deterministic normalization, selection, stale correlation, search, dependency navigation, and typed event/effect mapping pass focused and baseline checks."

[[task]]
id = "PKG-ADAPTER-001"
milestone = "M3"
title = "Acquire authoritative package data"
status = "DONE"
priority = 46
required = true
depends_on = ["PKG-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake pkgdata_adapter", "cargo test -p yoctui-app pkgdata_adapter"]
notes = "Validated shell-free oe-pkgdata-util discovery and exact batched commands return typed bounded inventory/detail data with unavailable fields, timeouts, cancellation, symlink/failure handling, and fake-process coverage. The opt-in live smoke was attempted against /home/bspguy-dev/src/bitbake/bitbake-builds/yoctui-qemux86-64/build but generated build/tmp/pkgdata is absent, so no live package-data compatibility is claimed."

[[task]]
id = "PKG-UI-001"
milestone = "M3"
title = "Integrate the package data workspace"
status = "DONE"
priority = 46
required = true
depends_on = ["PKG-ADAPTER-001"]
verify = ["cargo test -p yoctui-model pkgdata_workspace", "cargo test -p yoctui-app pkgdata_workspace", "cargo test -p yoctui-ui pkgdata_workspace", "cargo test -p yoctui -- pkgdata_workspace"]
notes = "Packages is a Navigator destination with correlated background inventory/detail execution, cancellation, search, stable selection, bounded dependency navigation history, exact recipe/provider actions, responsive explicit states, footer hints, and TestBackend coverage."

[[task]]
id = "IMAGES-001"
milestone = "M4"
title = "Complete Images artifact workspace"
status = "DONE"
priority = 49
required = true
depends_on = ["IMAGES-MODEL-001", "IMAGES-ADAPTER-001", "IMAGES-UI-001"]
verify = ["cargo test -p yoctui-model images_workspace", "cargo test -p yoctui-ui images_workspace", "cargo test -p yoctui-app image_action"]
notes = "Typed artifact state, authoritative bounded deploy acquisition, and responsive background workspace integration pass every child, parent, and baseline check while preserving the existing image picker and confirmed build flow. Live deployed-artifact compatibility is not claimed."

[[task]]
id = "IMAGES-MODEL-001"
milestone = "M4"
title = "Add typed image artifact state"
status = "DONE"
priority = 49
required = true
depends_on = ["JOB-002"]
verify = ["cargo test -p yoctui-model image_artifact_model", "cargo test -p yoctui-app image_artifact_model"]
notes = "Exact machine/image/path identities, typed available-versus-unavailable artifact metadata, deterministic bounds, correlated lifecycle states, identity-stable selection/search, reducer effects, and app event normalization pass focused and baseline checks."

[[task]]
id = "IMAGES-ADAPTER-001"
milestone = "M4"
title = "Acquire authoritative deployed image artifacts"
status = "DONE"
priority = 49
required = true
depends_on = ["IMAGES-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake image_artifact_adapter", "cargo test -p yoctui-app image_artifact_adapter"]
notes = "Tinfoil/environment snapshots expose DEPLOY_DIR_IMAGE, and the cancellable depth-one adapter validates machine/root identity, refuses symlinks and escapes, bounds entries/time/checksum input, classifies typed deploy records, parses checksum associations, and reports partial data explicitly. Focused and baseline checks pass."

[[task]]
id = "IMAGES-UI-001"
milestone = "M4"
title = "Integrate the responsive Images artifact workspace"
status = "DONE"
priority = 49
required = true
depends_on = ["IMAGES-ADAPTER-001"]
verify = ["cargo test -p yoctui-model images_workspace", "cargo test -p yoctui-app images_workspace", "cargo test -p yoctui-ui images_workspace", "cargo test -p yoctui -- images_workspace"]
notes = "Images now combines retained recipe picker/build confirmation with correlated CLI-owned scanning, search and exact selection, typed build/editor actions, explicit lifecycle/limitations, responsive Workspace/Inspector rendering, footer hints, and reducer/input/CLI/TestBackend coverage."

[[task]]
id = "QEMU-001"
milestone = "M4"
title = "Managed runqemu workflow"
status = "DONE"
priority = 50
required = true
depends_on = ["QEMU-MODEL-001", "QEMU-ADAPTER-001", "QEMU-UI-001"]
verify = ["cargo test -p yoctui-app qemu", "cargo test -p yoctui-ui qemu"]
notes = "The complete managed runqemu gate passes across exact artifact-bound model state, canonical capability inspection, independently revalidated shell-free arguments, bounded typed runner events, responsive dialogs/session history, and CLI-owned polling/cancellation. Fake executable coverage does not establish live runqemu compatibility."

[[task]]
id = "QEMU-MODEL-001"
milestone = "M4"
title = "Add typed runqemu launch and session state"
status = "DONE"
priority = 50
required = true
depends_on = ["JOB-002", "IMAGES-MODEL-001"]
verify = ["cargo test -p yoctui-model qemu_model", "cargo test -p yoctui-app qemu_model"]
notes = "Typed capability and exact artifact-bound launch drafts validate paths, identity, modes, memory, and bounded options before deterministic preview/confirmation. Stable QEMU sessions reuse disjoint shared background jobs for bounded typed output, lifecycle, failures, and confirmed cancellation; typed runner events normalize in the app without parsing."

[[task]]
id = "QEMU-ADAPTER-001"
milestone = "M4"
title = "Detect and execute runqemu safely"
status = "DONE"
priority = 50
required = true
depends_on = ["QEMU-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake qemu_adapter", "cargo test -p yoctui-app qemu_adapter"]
notes = "Canonical explicit/PATH capability inspection correlates exact regular non-symlink deployed images. Preview revalidation produces exact shell-free native arguments; the one-child bounded runner emits typed lifecycle/stream events and supports graceful process-group cancellation with forced escalation and rejection."

[[task]]
id = "QEMU-UI-001"
milestone = "M4"
title = "Integrate managed QEMU dialogs and session view"
status = "DONE"
priority = 50
required = true
depends_on = ["QEMU-UI-MODEL-001", "QEMU-UI-RENDER-001", "QEMU-UI-CLI-001"]
verify = ["cargo test -p yoctui-model qemu_workspace", "cargo test -p yoctui-app qemu_workspace", "cargo test -p yoctui-ui qemu_workspace", "cargo test -p yoctui -- qemu_workspace"]
notes = "The combined parent gate passes across bounded typed dialog state, modal app input, responsive TestBackend rendering, and independent CLI-owned capability/execution/cancellation integration. Session history and all terminal outcomes remain visible across navigation; fake runner evidence is not claimed as live compatibility."

[[task]]
id = "QEMU-UI-MODEL-001"
milestone = "M4"
title = "Add QEMU launch dialog and workspace input state"
status = "DONE"
priority = 50
required = true
depends_on = ["QEMU-ADAPTER-001"]
verify = ["cargo test -p yoctui-model qemu_workspace", "cargo test -p yoctui-app qemu_workspace"]
notes = "Pure typed launch rows provide read-only identity, bounded text editing, typed choice cycling, inline preview validation, focus-safe close/confirmation transitions, stable capability/selection/session disabled reasons, and Images/dialog key mapping."

[[task]]
id = "QEMU-UI-RENDER-001"
milestone = "M4"
title = "Render responsive QEMU launch and session UI"
status = "DONE"
priority = 50
required = true
depends_on = ["QEMU-UI-MODEL-001"]
verify = ["cargo test -p yoctui-ui qemu_workspace"]
notes = "Images renders capability/readiness and latest session lifecycle, exact request, timestamps, retained stream output, truncation/drop counts, result/error, and terminal outcomes before artifact details. Focus-trapped launch/preview/cancellation overlays and Q/x hints are safe at supported breakpoints."

[[task]]
id = "QEMU-UI-CLI-001"
milestone = "M4"
title = "Integrate QEMU capability and runner in the CLI"
status = "DONE"
priority = 50
required = true
depends_on = ["QEMU-UI-RENDER-001"]
verify = ["cargo test -p yoctui -- qemu_workspace"]
notes = "The CLI refreshes capability from each exact normalized image inventory, rebuilds and revalidates launch previews, owns one shell-free runner, routes modal input before global shortcuts, and independently polls typed success, nonzero, graceful/forced cancellation, rejection, and loss outcomes. Fake integration and baseline checks pass; live runqemu compatibility is not claimed."

[[task]]
id = "WIC-001"
milestone = "M4"
title = "Wic image workflow"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-UI-CLI-001", "WIC-WRITE-UI-CLI-001"]
verify = ["cargo test -p yoctui-model wic", "cargo test -p yoctui-bitbake wic", "cargo test -p yoctui-app wic", "cargo test -p yoctui-ui wic", "cargo test -p yoctui -- wic"]
notes = "The Wic parent gate passes the complete focused matrix and baseline. Typed state, safe cooked-mode capability/creation, responsive UI/CLI execution, exact output discovery, and independently rediscovered/revalidated protected device writing agree across model, app, adapters, UI, and CLI. Readiness-synchronized fake process/device coverage does not establish live Wic compatibility or removable-media safety."

[[task]]
id = "WIC-MODEL-001"
milestone = "M4"
title = "Add typed Wic creation and device-write state"
status = "DONE"
priority = 51
required = true
depends_on = ["JOB-002", "IMAGES-MODEL-001"]
verify = ["cargo test -p yoctui-model wic_model", "cargo test -p yoctui-app wic_model"]
notes = "Pure bounded Wic identities retain exact capability/output/device request correlation, normalized kickstart partitions and limitations, deterministic cooked-mode creation/write previews, disjoint shared-job history, stale-event rejection, all terminal outcomes, exact WRITE phrase gating, and the additional incomplete-device cancellation acknowledgement. Mechanical app event normalization and baseline checks pass."

[[task]]
id = "WIC-ADAPTER-001"
milestone = "M4"
title = "Discover kickstarts and execute Wic creation safely"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-ADAPTER-CAP-001", "WIC-ADAPTER-RUNNER-001"]
verify = ["cargo test -p yoctui-bitbake wic_adapter", "cargo test -p yoctui-app wic_adapter"]
notes = "The combined adapter gate passes canonical bounded capability/kickstart/partition inspection, capability-bound shell-free command revalidation, persistent process deadlines, bounded typed streams, graceful/forced cancellation, and nanosecond exact new-output snapshot comparison. Fake coverage passes; the active source has no live Wic installation."

[[task]]
id = "WIC-ADAPTER-CAP-001"
milestone = "M4"
title = "Inspect Wic capability and construct creation commands"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake wic_adapter_capability", "cargo test -p yoctui-app wic_adapter_capability"]
notes = "Canonical explicit/PATH Wic discovery executes bounded timed list-images inspection, merges configured and canned sources without following symlinks, returns bounded typed partition previews/limitations, and requires the current capability when independently revalidating every cooked-mode argument. Fake coverage passes; the active BitBake-only source has no live Wic installation."

[[task]]
id = "WIC-ADAPTER-RUNNER-001"
milestone = "M4"
title = "Run Wic creation and scan exact outputs"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-ADAPTER-CAP-001"]
verify = ["cargo test -p yoctui-bitbake wic_adapter_runner", "cargo test -p yoctui-app wic_adapter_runner"]
notes = "One-child process-group ownership emits bounded typed streams and distinct lifecycle outcomes, enforces a persistent execution deadline, rejects duplicates, supports graceful/forced cancellation, and compares bounded canonical non-symlink output snapshots with nanosecond change detection. Fake runner and baseline checks pass; live compatibility is not claimed."

[[task]]
id = "WIC-UI-MODEL-001"
milestone = "M4"
title = "Add Wic workspace dialog and input state"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-ADAPTER-001"]
verify = ["cargo test -p yoctui-model wic_workspace", "cargo test -p yoctui-app wic_workspace"]
notes = "Bounded modal creation rows provide read-only machine identity, typed image/kickstart/bmap/compression choices, output editing, validation and exact preview/confirmation, stable generated-output selection/opening, active Wic-before-QEMU cancellation routing, focus restoration, disabled reasons, and distinct upper/lower-case Wic key mapping."

[[task]]
id = "WIC-UI-RENDER-001"
milestone = "M4"
title = "Render responsive Wic creation and output UI"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-UI-MODEL-001"]
verify = ["cargo test -p yoctui-ui wic_workspace"]
notes = "Responsive Images rendering now covers every typed Wic capability, output inventory, job lifecycle and terminal state; exact request/result/error/stream/drop details; selected output metadata; syntax-highlighted kickstart source and typed partitions; modal creation/preview/cancellation; validation/edit/read-only state; compact 80x24 footer hints; long content; and all semantic themes. Focused and baseline checks pass."

[[task]]
id = "WIC-UI-CLI-001"
milestone = "M4"
title = "Integrate Wic creation capability and runner in the CLI"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-UI-RENDER-001"]
verify = ["cargo test -p yoctui -- wic_workspace"]
notes = "The CLI now generation-correlates asynchronous capability inspection from authoritative WKS workspace values, routes Wic modals before global input, independently rebuilds/revalidates exact creation previews, owns and nonblockingly polls one kill-on-drop runner, scans exact outputs, and handles success, nonzero failure, graceful/forced cancellation, rejection, duplicate prevention, and loss while preserving history across navigation. Fake integration and baseline checks pass; live Wic compatibility is not claimed."

[[task]]
id = "WIC-WRITE-ADAPTER-001"
milestone = "M4"
title = "Discover and revalidate safe Wic write devices"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-ADAPTER-001"]
verify = ["cargo test -p yoctui-bitbake wic_device_write", "cargo test -p yoctui-app wic_device_write"]
notes = "Canonical bounded lsblk JSON discovery validates every record, fails closed on uncertain root identity, exposes only writable removable whole devices, and reports bounded exclusions. Exact image/device identity and every safety invariant are rediscovered immediately before the shell-free wic write spawn. Fake-device/process tests cover malformed/oversized/duplicate/unsafe/stale records, exact argv, timeout, nonzero failure, cancellation, truncation, and loss without claiming live hardware safety."

[[task]]
id = "WIC-WRITE-MODEL-001"
milestone = "M4"
title = "Add protected Wic device dialog state"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-WRITE-ADAPTER-001", "WIC-UI-MODEL-001"]
verify = ["cargo test -p yoctui-model wic_device_write", "cargo test -p yoctui-app wic_device_write"]
notes = "Exact generated/deployed uncompressed image resolution now opens correlated discovery; full device identity survives rediscovery; typed picker, bounded phrase, separate exact preview, final stale-state reconstruction, and write-specific cancellation acknowledgement are covered by reducer and app-input tests. Compressed deployed Wic files are no longer classified as write-capable."

[[task]]
id = "WIC-WRITE-RENDER-001"
milestone = "M4"
title = "Render protected Wic device workflow"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-WRITE-MODEL-001"]
verify = ["cargo test -p yoctui-ui wic_device_write"]
notes = "The Images workspace now renders correlated loading/empty/available/partial/failed device inventories, exact whole-device identity and limitations, distinct bounded phrase and indexed destructive argv previews, write readiness/history/telemetry/output, a stronger incomplete-device cancellation warning, and full/compact D footer hints. Focused TestBackend coverage passes at 80x24 and across semantic themes/no-color; no live removable-media claim is made."

[[task]]
id = "WIC-WRITE-CLI-001"
milestone = "M4"
title = "Integrate protected Wic device execution in the CLI"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-WRITE-RENDER-001"]
verify = ["cargo test -p yoctui -- wic_device_write"]
notes = "The CLI now owns replaceable generation-correlated device discovery plus a nonblocking write-start task that repeats adapter-authoritative image/device validation before spawn. Modal routing, shared runner polling, telemetry, success, nonzero failure, stronger cancellation, rejection, stale identity, startup/runner loss, and fake device/process outcomes pass focused coverage; production never disables node validation and no live removable-media claim is made."

[[task]]
id = "WIC-WRITE-UI-CLI-001"
milestone = "M4"
title = "Integrate protected Wic device writing"
status = "DONE"
priority = 51
required = true
depends_on = ["WIC-WRITE-MODEL-001", "WIC-WRITE-RENDER-001", "WIC-WRITE-CLI-001"]
verify = ["cargo test -p yoctui-model wic_device_write", "cargo test -p yoctui-app wic_device_write", "cargo test -p yoctui-ui wic_device_write", "cargo test -p yoctui -- wic_device_write"]
notes = "The cross-layer parent gate passes together: fail-closed adapter discovery/revalidation, exact typed model state, modal app routing, responsive TestBackend rendering, and independently polled CLI discovery/start/write/cancellation all agree. Synthetic device/process tests cover terminal outcomes but do not establish live removable-media compatibility."

[[task]]
id = "SDK-MODEL-001"
milestone = "M4"
title = "Model typed SDK workspace and operations"
status = "DONE"
priority = 52
required = true
depends_on = ["JOB-002"]
verify = ["cargo test -p yoctui-model sdk_workflow", "cargo test -p yoctui-app sdk_workflow"]
notes = "SDK is now a first-class typed Navigator destination. Pure state covers exact populate/test BuildRequest previews, bounded artifact identities and correlated inventory/search/selection, publication/native capability/drafts/indexed previews, shared Sdk background-job sessions, lifecycle/output/cancellation/loss, stale-event rejection, and modal app key mapping. Focused and baseline tests pass."

[[task]]
id = "SDK-ARTIFACT-001"
milestone = "M4"
title = "Discover authoritative SDK artifacts"
status = "DONE"
priority = 52
required = true
depends_on = ["SDK-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake sdk_artifact"]
notes = "A cancellable blocking adapter now validates the exact canonical SDK deploy root, traverses deterministic directory/record/path bounds without following links, classifies and associates typed regular records, preserves size/mtime, leaves non-authoritative metadata unavailable, and distinguishes empty, partial, root/permission failure, timeout, cancellation, and worker loss. Seven fake-filesystem tests and all baseline checks pass; live SDK compatibility is not claimed."

[[task]]
id = "SDK-TOOLS-001"
milestone = "M4"
title = "Adapt SDK publication and native tools"
status = "DONE"
priority = 52
required = true
depends_on = ["SDK-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake sdk_tool"]
notes = "The SDK tool adapter now discovers each canonical workspace tool independently, revalidates exact publication/native previews and identities, rejects stale installers/nonempty destinations/symlinks, parses one bounded strict extracted-SDK environment into a child-only map, and runs exact shell-free commands with bounded typed output, timeout, loss, nonzero, rejection, and graceful/forced cancellation outcomes. Eight fake-filesystem/process tests and all baseline checks pass; live SDK compatibility is not claimed."

[[task]]
id = "SDK-RENDER-001"
milestone = "M4"
title = "Render responsive SDK workspace"
status = "DONE"
priority = 52
required = true
depends_on = ["SDK-MODEL-001"]
verify = ["cargo test -p yoctui-ui sdk_workflow"]
notes = "SDK now renders a typed responsive Workspace and Inspector with every inventory state, exact selection/metadata/associations/limitations, capability and managed-session history, output and terminal outcomes. All six existing SDK dialogs show validation and exact previews; narrow shortcuts, all themes, no-color semantics, long inputs, and 80x24 boundaries pass focused TestBackend and baseline checks."

[[task]]
id = "SDK-CLI-001"
milestone = "M4"
title = "Integrate SDK execution in the CLI"
status = "DONE"
priority = 52
required = true
depends_on = ["SDK-ARTIFACT-001", "SDK-TOOLS-001", "SDK-RENDER-001"]
verify = ["cargo test -p yoctui -- sdk_workflow"]
notes = "The CLI now constructs SDK adapters from canonical active workspace identities, asynchronously inspects individual tools, owns replaceable correlated scans and one shell-free tool runner, routes populate/test through the existing BitBake coordinator, refreshes only after successful populate/publication, and independently polls output, timeout, cancellation/rejection, failure, and loss while navigation and telemetry continue. Focused fake integration and baseline checks pass; live SDK compatibility is not claimed."

[[task]]
id = "SDK-UI-CLI-001"
milestone = "M4"
title = "Integrate complete SDK workspace"
status = "DONE"
priority = 52
required = true
depends_on = ["SDK-MODEL-001", "SDK-ARTIFACT-001", "SDK-TOOLS-001", "SDK-RENDER-001", "SDK-CLI-001"]
verify = ["cargo test -p yoctui-model sdk_workflow", "cargo test -p yoctui-app sdk_workflow", "cargo test -p yoctui-bitbake sdk_", "cargo test -p yoctui-ui sdk_workflow", "cargo test -p yoctui -- sdk_workflow"]
notes = "The cross-layer SDK workspace gate now passes: every documented shortcut reaches typed model state through the real CLI router, exact selected artifacts open through the configured editor, and the native-tool dialog provides bounded keyboard-editable mode/workspace/recipe/tool/argument fields before an indexed shell-free preview. Managed builds, correlated scans, publication/native child jobs, independent cancellation, navigation retention, refresh, terminal outcomes, long inputs, and 80x24 rendering pass focused and baseline coverage. Cancellation fixtures synchronize readiness deterministically; fake filesystems/processes do not claim live SDK compatibility."

[[task]]
id = "SDK-001"
milestone = "M4"
title = "SDK build and artifact workflow"
status = "DONE"
priority = 52
required = true
depends_on = ["SDK-UI-CLI-001"]
verify = ["cargo test -p yoctui-model sdk_workflow", "cargo test -p yoctui-app sdk_workflow", "cargo test -p yoctui-bitbake sdk_", "cargo test -p yoctui-ui sdk_workflow", "cargo test -p yoctui -- sdk_workflow"]
notes = "The SDK parent gate passes the complete focused matrix and baseline at the committed cross-layer state. Populate SDK/ext SDK and contextual testsdk launches reuse the managed BitBake coordinator; correlated artifacts, configured-editor opening, shell-free publication and extracted/native execution, cancellation, refresh, responsive dialogs, and terminal outcomes agree across model, app, adapters, UI, and CLI. Fake filesystem/process evidence does not establish live SDK compatibility."

[[task]]
id = "TEST-SPEC-001"
milestone = "M5"
title = "Specify unified testing workflows"
status = "DONE"
priority = 60
required = true
depends_on = ["JOB-002", "SDK-001"]
verify = ["./scripts/verify-roadmap.sh"]
notes = "The authoritative contract now defines Testing navigation and responsive views; typed OE/BitBake selftest selectors; exact testimage/testsdk/testsdkext BuildRequests; configuration-gated ptest; stable background sessions; explicit structured-result states; exact regression categories; result/log opening; non-overwriting JUnit export; cancellation and failure meaning; and model/adapter/app/UI/CLI ownership. It follows current official Yocto test roles and keeps mocked evidence separate from live compatibility."

[[task]]
id = "TEST-MODEL-001"
milestone = "M5"
title = "Model typed test discovery and execution"
status = "DONE"
priority = 60
required = true
depends_on = ["TEST-SPEC-001"]
verify = ["cargo test -p yoctui-model test_workflow", "cargo test -p yoctui-app test_workflow"]
notes = "Typed capability, family catalog, bounded launch form, exact previews, stable managed sessions, background-job attachment, output, cancellation/rejection, success/failure/loss, stale-event rejection, app input mapping, Testing navigation, and responsive reachability now pass focused and baseline checks. Selftests retain validated executable/argv identity while testimage, testsdk, testsdkext, and configured ptest reuse exact BuildRequests; no live compatibility is claimed."

[[task]]
id = "TEST-RESULT-MODEL-001"
milestone = "M5"
title = "Model typed test results and comparisons"
status = "DONE"
priority = 60
required = true
depends_on = ["TEST-MODEL-001"]
verify = ["cargo test -p yoctui-model test_results", "cargo test -p yoctui-app test_results"]
notes = "Bounded canonical identity snapshots, deterministic suite/case/metadata normalization, explicit import states, identity-stable search/drill selections, exact result/log effects, regression/new-failure/new-pass/removed comparison, correlated terminal responses, and two-stage non-overwriting JUnit destination validation pass focused and baseline checks. Exact regression-file and JUnit vectors follow the supported resulttool interface; adapters must still revalidate filesystem identity immediately before spawn."

[[task]]
id = "TEST-RUNNER-ADAPTER-001"
milestone = "M5"
title = "Adapt typed Yocto test execution"
status = "DONE"
priority = 60
required = true
depends_on = ["TEST-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake test_runner", "cargo test -p yoctui-app test_runner"]
notes = "The Testing runner now discovers canonical oe-selftest and bitbake-selftest identities from an explicit initialized PATH snapshot, preserves typed ptest configuration, reconstructs exact shell-free argv, revalidates executable/build identities at spawn, confines BB_SKIP_NETTESTS=yes to BitBake-selftest children, and emits bounded stream-tagged lifecycle events. Fake-process tests cover tampering, duplicates, success, nonzero, loss, cancellation rejection, graceful/forced cancellation, and timeout; managed BitBake families remain on the existing coordinator and no live compatibility is claimed."

[[task]]
id = "TEST-RESULT-ADAPTER-001"
milestone = "M5"
title = "Adapt resulttool import comparison and export"
status = "DONE"
priority = 60
required = true
depends_on = ["TEST-RESULT-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake test_results", "cargo test -p yoctui-app test_results"]
notes = "Independent canonical PATH discovery, bounded canonical non-symlink testresults.json import, official-shaped typed parsing, SHA-256 result identities, exact shell-free regression-file/JUnit vectors, immediate executable/result/destination revalidation, non-overwriting export, bounded stream events, and identity-correlated terminal outcomes pass focused and baseline checks. Fake filesystem/process tests cover empty, partial, malformed, oversized, tampering, nonzero, cancellation, timeout, rejection, and worker loss; no live compatibility is claimed."

[[task]]
id = "TEST-RENDER-001"
milestone = "M5"
title = "Render responsive Testing workspace"
status = "DONE"
priority = 60
required = true
depends_on = ["TEST-RESULT-MODEL-001"]
verify = ["cargo test -p yoctui-ui test_workflow"]
notes = "The complete typed Launches, Results, and Comparison views now render exact configuration/tool/result identities, lifecycle and every terminal state, bounded suite/case drill-down, metadata, limitations, related logs, semantic outcome/regression styling, JUnit status, compact/full footers, and all seven focus-trapped launch/import/comparison/export/cancellation dialogs. TestBackend coverage passes at 80x24, medium, wide, every theme/no-color mode, and the existing too-small gate without parsing backend text."

[[task]]
id = "TEST-CLI-001"
milestone = "M5"
title = "Integrate testing execution in the CLI"
status = "DONE"
priority = 60
required = true
depends_on = ["TEST-RUNNER-ADAPTER-001", "TEST-RESULT-ADAPTER-001", "TEST-RENDER-001"]
verify = ["cargo test -p yoctui -- test_workflow"]
notes = "The CLI snapshots initialized Testing capabilities, routes image/SDK/ptest tasks through the existing managed BitBake coordinator, and independently owns one selftest runner, one replaceable bounded result import, and one resulttool comparison/export runner. Exact session/job/result identities survive capability discovery, execution, navigation, cancellation, import, comparison, JUnit destination inspection/export, refresh, and every typed terminal outcome. Focused CLI tests and the complete baseline pass; fake adapters do not establish live Yocto compatibility."

[[task]]
id = "TEST-UI-CLI-001"
milestone = "M5"
title = "Integrate complete Testing workspace"
status = "DONE"
priority = 60
required = true
depends_on = ["TEST-MODEL-001", "TEST-RESULT-MODEL-001", "TEST-RUNNER-ADAPTER-001", "TEST-RESULT-ADAPTER-001", "TEST-RENDER-001", "TEST-CLI-001"]
verify = ["cargo test -p yoctui-model test_workflow", "cargo test -p yoctui-app test_workflow", "cargo test -p yoctui-bitbake test_", "cargo test -p yoctui-ui test_workflow", "cargo test -p yoctui -- test_workflow"]
notes = "The cross-layer gate passes for every specified launch family, exact result import and suite/case drill-down, typed comparison, non-overwriting JUnit export, cancellation and every terminal state, responsive rendering, navigation retention, managed BitBake reuse, and independently polled CLI execution. All focused layer checks and the complete baseline pass; live Yocto test compatibility remains unclaimed."

[[task]]
id = "TEST-001"
milestone = "M5"
title = "Unified test execution and results"
status = "DONE"
priority = 60
required = true
depends_on = ["TEST-UI-CLI-001"]
verify = ["cargo test -p yoctui-model test_workflow", "cargo test -p yoctui-app test_workflow", "cargo test -p yoctui-bitbake test_", "cargo test -p yoctui-ui test_workflow", "cargo test -p yoctui -- test_workflow"]
notes = "The unified Testing parent gate passes after specification, typed execution/result state, selftest and managed BitBake/ptest runners, resulttool import/comparison/JUnit export, responsive rendering, and non-blocking CLI integration completed. All focused and baseline checks pass; fake-process coverage is not described as live Yocto compatibility."

[[task]]
id = "SEC-SPEC-001"
milestone = "M5"
title = "Specify typed CVE and SPDX workflows"
status = "DONE"
priority = 61
required = true
depends_on = ["JOB-002"]
verify = ["./scripts/verify-roadmap.sh"]
notes = "The authoritative contracts now define a first-class responsive Security workspace; CVE findings, filters, exact advisory/report/provider routes, checks and package mapping; legacy/current recipe and image SBOM capability; bounded exact SPDX summaries; imports and authoritative scans; managed lifecycle/cancellation; every explicit partial/terminal state; and model/adapter/app/UI/CLI ownership. Task and path support is capability-driven, and mocked evidence remains separate from live compatibility."

[[task]]
id = "SEC-MODEL-001"
milestone = "M5"
title = "Model typed security workflows"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-SPEC-001"]
verify = ["cargo test -p yoctui-model security_workflow", "cargo test -p yoctui-app security_workflow"]
notes = "A first-class Security destination now owns validated exact recipe/image scope, capability-supplied current/legacy task choices, deterministic CVE/SBOM/package-map previews, stable sessions and managed-job identity, cancellation and terminal outcomes, canonical report identities, bounded normalized CVE findings/mappings and SPDX summaries, explicit correlated inventory states, search/filter/drill selection, exact open effects, focus-trapped typed dialogs, and mechanical app key mapping. Focused and baseline checks pass without filesystem or process parsing in the reducer."

[[task]]
id = "SEC-CAP-ADAPTER-001"
milestone = "M5"
title = "Inspect Security capabilities"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake security_capability"]
notes = "The fail-closed inspector accepts only explicit initialized metadata, canonicalizes and validates the primary build/scope identity, bounds and de-duplicates optional report/PATH roots with visible limitations, discovers only a canonical regular executable cve-check-map-pkgs, and preserves exact reported current create_recipe_sbom or legacy create_spdx plus cve_check/image behavior without consulting release text or process-global environment. Current, legacy, missing, partial, symlink, invalid-scope, and excessive-input fixtures pass focused and baseline checks."

[[task]]
id = "SEC-REPORT-ADAPTER-001"
milestone = "M5"
title = "Acquire and parse Security reports"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-CAP-ADAPTER-001"]
verify = ["cargo test -p yoctui-bitbake security_report"]
notes = "The replaceable blocking adapter accepts only sorted unique explicit canonical regular files or bounded canonical directories, never follows symlinks or escapes a root, bounds traversal/files/bytes/records/fields/time, revalidates metadata around acquisition, and fingerprints exact bytes. Supported Yocto-shaped CVE JSON/text and SPDX JSON become typed findings/documents/components; archives and unsupported SPDX schemas remain exact partial artifacts. Empty, mixed partial, malformed, oversized, missing, symlink, escape, stale, cancellation, timeout, and worker-loss outcomes are explicit. Seven focused fake-filesystem tests plus the complete baseline pass."

[[task]]
id = "SEC-MAPPER-ADAPTER-001"
milestone = "M5"
title = "Run exact CVE package mapping"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-CAP-ADAPTER-001"]
verify = ["cargo test -p yoctui-bitbake security_mapper", "cargo test -p yoctui-app security_workflow"]
notes = "A session-correlated independent runner reconstructs only the exact typed cve-check-map-pkgs preview, captures and immediately revalidates canonical regular non-symlink executable/input identities, spawns native argv in a child process group, bounds lossy stdout/stderr and retained model output, and preserves started, output, success, nonzero, cancellation requested/rejected/graceful/forced, timeout, and worker-loss events. Tampering, duplicate starts, stale tools, symlinked tools/inputs, invalid arguments, and wrong-session cancellation fail closed. Focused fake-process and baseline checks pass; live compatibility is not claimed."

[[task]]
id = "SEC-ADAPTER-001"
milestone = "M5"
title = "Adapt CVE and SPDX metadata and reports"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-REPORT-ADAPTER-001", "SEC-MAPPER-ADAPTER-001"]
verify = ["cargo test -p yoctui-bitbake security", "cargo test -p yoctui-app security_workflow"]
notes = "The parent gate now passes across canonical capability discovery, exact current/legacy task preservation, bounded fingerprinted CVE/SPDX acquisition/parsing, archive/unsupported-schema retention, and the shell-free identity-revalidated package-mapping runner. All boundaries expose typed empty/partial/failure/cancellation/timeout/loss meaning, and the app maps mapper events mechanically. Fifteen focused adapter tests, focused app tests, and the baseline pass. Coverage is fake process/filesystem evidence only and does not claim live Yocto Security compatibility."

[[task]]
id = "SEC-RENDER-001"
milestone = "M5"
title = "Render responsive Security workspace"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-MODEL-001"]
verify = ["cargo test -p yoctui-ui security_workflow"]
notes = "The Security destination now renders typed CVE and SBOM views, exact finding/report/component identity and metadata, capability and inventory states, partial limitations, bounded mapper lifecycle/output, and focus-trapped operation/import/cancellation dialogs. Wide, medium, 80x24, long-field, theme, no-color, drill, and every explicit terminal-state TestBackend fixtures pass together with the full baseline."

[[task]]
id = "SEC-CLI-001"
milestone = "M5"
title = "Integrate security execution in the CLI"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-ADAPTER-001", "SEC-RENDER-001"]
verify = ["cargo test -p yoctui -- security_workflow"]
notes = "The CLI snapshots the initialized build/PATH inputs, discovers typed capability without process-global mutation, routes CVE/SBOM requests through the shared managed build coordinator, and independently owns one mapper plus one replaceable report scan. Exact report/provider/advisory opens revalidate before shell-free launch; generation/session correlation, refresh, navigation, duplicate rejection, cancellation, empty/partial/failure/timeout/loss, build outcomes, and bounded output pass seven focused fake integration tests and the baseline. No live compatibility is claimed."

[[task]]
id = "SEC-UI-CLI-001"
milestone = "M5"
title = "Integrate complete Security workspace"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-MODEL-001", "SEC-ADAPTER-001", "SEC-RENDER-001", "SEC-CLI-001"]
verify = ["cargo test -p yoctui-model security_workflow", "cargo test -p yoctui-app security_workflow", "cargo test -p yoctui-bitbake security", "cargo test -p yoctui-ui security_workflow", "cargo test -p yoctui -- security_workflow"]
notes = "The cross-layer gate passes with exact capability-driven current/legacy CVE and SBOM previews, normalized findings/mappings and SPDX summaries, bounded acquisition, shell-free mapping, responsive rendering, managed BitBake reuse, independent CLI polling, refresh, exact-open revalidation, cancellation, navigation, and every explicit terminal outcome. Focused evidence is 4 model, 2 app, 15 adapter, 5 UI, and 7 CLI tests plus the baseline; fixtures do not claim live compatibility."

[[task]]
id = "SEC-001"
milestone = "M5"
title = "CVE and SPDX workflows"
status = "DONE"
priority = 61
required = true
depends_on = ["SEC-UI-CLI-001"]
verify = ["cargo test -p yoctui-model security_workflow", "cargo test -p yoctui-app security_workflow", "cargo test -p yoctui-bitbake security", "cargo test -p yoctui-ui security_workflow", "cargo test -p yoctui -- security_workflow"]
notes = "The complete Security workspace passes its specification, typed state, capability/report/mapper adapter, responsive rendering, non-blocking CLI, and cross-layer gates. CVE checks, package mapping, recipe/image SBOM generation, report import/refresh, exact evidence inspection/opening, managed lifecycle, cancellation, and partial/terminal states are covered without converting fixture evidence into a live Yocto claim."

[[task]]
id = "QA-SPEC-001"
milestone = "M5"
title = "Specify unified QA workflows"
status = "DONE"
priority = 62
required = true
depends_on = ["JOB-002"]
verify = ["./scripts/verify-roadmap.sh"]
notes = "The authoritative contracts now define a responsive QA destination with typed Recipe & Kernel and Layer QA views; exact recipe/provider/configured-layer scope; capability-supplied kernel, URI, patch, license, recipe/package, and yocto-check-layer catalog entries; indexed confirmations; managed BitBake reuse; one exact native layer runner; bounded findings/reports/imports; exact opens; cancellation; explicit partial/terminal states; responsive footer/dialog behavior; and model/adapter/app/UI/CLI ownership. Existing Recipes task and patch-review routes remain contextual, and fixture evidence remains separate from live compatibility."

[[task]]
id = "QA-CHECK-MODEL-001"
milestone = "M5"
title = "Model typed recipe and kernel QA checks"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-SPEC-001"]
verify = ["cargo test -p yoctui-model qa_check_workflow", "cargo test -p yoctui-app qa_workflow"]
notes = "Pure typed QA state now validates exact recipe/provider scope and a capability-supplied kernel, URI, patch, license, and recipe/package catalog without task guessing. It owns indexed stale-safe managed-BitBake previews, stable bounded session/output history, background-job attachment and cancellation, every terminal outcome, generation-correlated bounded report identities/findings/imports, empty/partial/failure states, search/status selection and drill, exact opens, focus-trapped dialogs, reducer coverage, and mechanical app input mapping."

[[task]]
id = "QA-LAYER-MODEL-001"
milestone = "M5"
title = "Model typed layer QA checks"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-SPEC-001"]
verify = ["cargo test -p yoctui-model qa_layer_workflow", "cargo test -p yoctui-app qa_workflow"]
notes = "The QA reducer now retains every exact configured layer, including disabled rows, and models not-inspected/inspecting/available/partial/failed capability states with stable reasons. Canonical executable identities and capability-supplied native vectors drive indexed stale-safe previews, an independent bounded session/output history, exact cancellation, nonzero/success/cancel/timeout/loss outcomes, correlated layer findings/counts/reports, search/filter/drill/navigation, focus-trapped dialogs, and exact root/report/source effects. Recipe/kernel and native layer cancellation targets remain independent."

[[task]]
id = "QA-MODEL-001"
milestone = "M5"
title = "Close typed QA model gate"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-CHECK-MODEL-001", "QA-LAYER-MODEL-001"]
verify = ["cargo test -p yoctui-model qa_", "cargo test -p yoctui-app qa_workflow"]
notes = "The combined model/app parent gate passes 18 QA-focused model tests and the complete app mapping gate. Recipe/provider and configured-layer capability state, catalog/vector identities, deterministic previews, independent managed/native sessions and cancellation, bounded output/history/reports/findings, exact configured scope validation, filters/drill/navigation, focus-trapped dialogs, exact opens, and every terminal outcome agree with the UI and architecture contracts without host, report, process, or widget parsing."

[[task]]
id = "QA-TASK-ADAPTER-001"
milestone = "M5"
title = "Adapt recipe and kernel QA capabilities"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake qa_task"]
notes = "A fail-closed adapter now validates canonical build/provider/report-root identities and bounded authoritative scope/task inventories, exposes every required family, enables only exact explicitly reported task bindings, preserves optional unsafe inputs as typed partial limitations, and mechanically maps responses across the app boundary. Alternate tasks are accepted only as explicit data, kernel capability requires explicit classification, and fixture coverage makes no live Yocto claim."

[[task]]
id = "QA-REPORT-ADAPTER-001"
milestone = "M5"
title = "Acquire bounded QA findings and reports"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake qa_report"]
notes = "The QA report adapter now accepts exact managed or import candidates with producer/scope authority, confines managed roots, bounds traversal/files/bytes/records/fields/time, parses documented JSON/JSONL/XML/tab-text/BitBake QA records, fingerprints and revalidates identities, normalizes typed findings, and preserves empty, partial, malformed, missing, permission, stale, cancellation, timeout, and loss outcomes. Mechanical app mapping is covered; fake filesystem evidence makes no live Yocto claim."

[[task]]
id = "QA-LAYER-ADAPTER-001"
milestone = "M5"
title = "Run exact layer QA checks"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-LAYER-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake qa_layer", "cargo test -p yoctui-app qa_workflow"]
notes = "The layer-QA adapter discovers only a canonical executable from the child search path, retains every exact configured layer with disabled reasons, constructs one exact layer-root argv, validates report roots, captures and revalidates executable/layer identities before shell-free spawn, and streams bounded typed output. Fake-process coverage preserves success, nonzero, duplicate, graceful/forced cancellation, rejection, timeout, stale identity, and channel-loss outcomes without claiming live compatibility."

[[task]]
id = "QA-ADAPTER-001"
milestone = "M5"
title = "Close QA adapter gate"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-TASK-ADAPTER-001", "QA-REPORT-ADAPTER-001", "QA-LAYER-ADAPTER-001"]
verify = ["cargo test -p yoctui-bitbake qa_", "cargo test -p yoctui-app qa_workflow"]
notes = "The combined adapter/app parent gate passes 17 BitBake adapter tests and five mechanical app tests. Authoritative task capability, bounded fingerprinted findings, exact shell-free layer execution, independent cancellation targets, and every applicable empty/partial/failure/nonzero/cancel/timeout/loss outcome agree without widget parsing or mocked live claims."

[[task]]
id = "QA-RENDER-001"
milestone = "M5"
title = "Render responsive QA workspace"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-MODEL-001"]
verify = ["cargo test -p yoctui-ui qa_workflow"]
notes = "QA is now a first-class Navigator destination after Security. Typed Recipe & Kernel and Layer QA views render capability, catalog/layer rows, status counts, reports, findings, sessions/output, exact Inspector identities, limitations, every report terminal state, all confirmation/import/cancellation dialogs, full/compact footers, themes, no-color, and responsive 80x24 behavior. Three focused TestBackend tests and the full UI suite pass."

[[task]]
id = "QA-CLI-001"
milestone = "M5"
title = "Integrate non-blocking QA execution"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-ADAPTER-001", "QA-RENDER-001"]
verify = ["cargo test -p yoctui -- qa_workflow"]
notes = "The CLI now inspects exact recipe/provider/task and configured-layer/tool capability, reuses the managed BitBake coordinator, independently owns one native layer runner and one replaceable report worker, and revalidates every exact open. Seven focused integration tests cover managed/native execution, independent cancellation, report generations and terminal states, navigation, key/modal routing, and stale report/provider/source/layer rejection. Full baseline verification passes; fake fixtures do not claim live compatibility."

[[task]]
id = "QA-UI-CLI-001"
milestone = "M5"
title = "Integrate complete QA workspace"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-MODEL-001", "QA-ADAPTER-001", "QA-RENDER-001", "QA-CLI-001"]
verify = ["cargo test -p yoctui-model qa_", "cargo test -p yoctui-app qa_workflow", "cargo test -p yoctui-bitbake qa_", "cargo test -p yoctui-ui qa_workflow", "cargo test -p yoctui -- qa_workflow"]
notes = "The complete cross-layer QA matrix passes together: 18 model, five app, 17 adapter, three responsive TestBackend, and seven CLI tests verify capability-driven recipe/kernel and configured-layer checks, exact identities, managed/native execution, independent cancellation, bounded reports, revalidated opens, navigation, dialogs, responsive rendering, and every terminal state. The full baseline passes; fixture evidence does not claim live compatibility."

[[task]]
id = "QA-001"
milestone = "M5"
title = "Recipe, kernel, and layer QA workflows"
status = "DONE"
priority = 62
required = true
depends_on = ["QA-UI-CLI-001"]
verify = ["cargo test -p yoctui-model qa_", "cargo test -p yoctui-app qa_workflow", "cargo test -p yoctui-bitbake qa_", "cargo test -p yoctui-ui qa_workflow", "cargo test -p yoctui -- qa_workflow"]
notes = "The QA parent gate passes from the committed cross-layer state: capability-driven recipe/kernel and configured-layer checks, exact identities, bounded reports, responsive rendering, managed BitBake reuse, independent native execution/cancellation, revalidated evidence opens, navigation, and every terminal outcome agree across all crates. The full baseline passes; fake fixtures do not establish live Yocto compatibility."

[[task]]
id = "MAINT-SPEC-001"
milestone = "M6"
title = "Specify typed Maintenance workflows"
status = "DONE"
priority = 70
required = true
depends_on = ["JOB-002", "DIALOG-001"]
verify = ["./scripts/verify-roadmap.sh"]
notes = "The authoritative UI and architecture contracts now define one responsive four-view Maintenance destination, current-versus-legacy tool capability, exact identities, typed readiness/cleanup/service/release/integration workflows, separate destructive/network confirmations, lifecycle, evidence, and fake-versus-live boundaries without duplicating Signatures, Security, QA, or recipe patch review."

[[task]]
id = "MAINT-MODEL-001"
milestone = "M6"
title = "Model typed Maintenance state and operations"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-SPEC-001"]
verify = ["cargo test -p yoctui-model maintenance_workflow", "cargo test -p yoctui-app maintenance_workflow"]
notes = "First-class Maintenance model state now owns four fixed views, per-view selection, bounded exact capability/metadata/service state, every typed operation request and preview, protected cleanup phrase, separate network confirmation, one correlated session lifecycle, replaceable evidence, cancellation, all terminal outcomes, and routes to existing owned workspaces. Six reducer and two app tests pass with the full baseline."

[[task]]
id = "MAINT-SSTATE-ADAPTER-001"
milestone = "M6"
title = "Adapt sstate readiness and protected cleanup"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake maintenance_sstate"]
notes = "The adapter distinguishes current Python and legacy shell cleanup interfaces, constructs exact readiness and cleanup vectors, bounds exact preview candidates, revalidates every identity before execution, and covers success, nonzero failure, timeout, graceful/forced cancellation, rejection, and runner loss. Seven focused adapter tests and the complete baseline pass; fixture evidence does not claim live sstate safety."

[[task]]
id = "MAINT-SERVICE-ADAPTER-001"
milestone = "M6"
title = "Adapt PR and hash service diagnostics"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake maintenance_service"]
notes = "Typed primary/upstream endpoint roles retain local/remote and reachable/unreachable/configured/disabled/partial/unavailable meaning, while bounded PID/executable evidence for bitbake-prserv, bitbake-hashserv, and bitbake-worker remains observational only. The canonical bitbake-prserv-tool exposes only exact export/import vectors with build/endpoint context, native side-effect warnings, source/destination and executable revalidation, and the shared bounded process-group runner. Six adapter tests plus model and complete baseline checks pass; fixtures do not claim live service health or PR database compatibility."

[[task]]
id = "MAINT-RELEASE-ADAPTER-001"
milestone = "M6"
title = "Adapt locked signatures comparisons and archives"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake maintenance_release"]
notes = "Canonical non-symlink discovery and exact shell-free vectors now cover gen-lockedsig-cache, buildhistory-diff, and oe-git-archive with immediate executable/input/output/Git identity revalidation, bounded replaceable evidence, and the shared timeout/cancellation/loss-aware runner. Archive creation and remote push are separate operations linked by a revalidated local-result identity. A discovered build-compare remains explicitly unavailable because its obsolete distinct interface is not safely interchangeable with buildhistory-diff. Seven adapter tests, one model test, and the complete baseline pass; fixture evidence does not claim live tool or network compatibility."

[[task]]
id = "MAINT-OPTIONAL-ADAPTER-001"
milestone = "M6"
title = "Detect optional release integrations"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake maintenance_optional"]
notes = "A detection-only adapter now reports complete, partial, and unavailable pull-request, error-report, repo-manifest, and Toaster integrations. It retains canonical build/helper/Git HEAD/report/repo workspace/manifest/configuration identities, bounds all candidates and process evidence, revalidates stale evidence, rejects unsafe helper links and escaped manifests, and labels Toaster process names as observational only. It constructs no command and performs no mail, upload, network, manifest mutation, or service lifecycle action. Seven focused tests and the complete baseline pass; fixtures do not claim live integration compatibility."

[[task]]
id = "MAINT-ADAPTER-001"
milestone = "M6"
title = "Close Maintenance adapter gate"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-SSTATE-ADAPTER-001", "MAINT-SERVICE-ADAPTER-001", "MAINT-RELEASE-ADAPTER-001", "MAINT-OPTIONAL-ADAPTER-001"]
verify = ["cargo test -p yoctui-bitbake maintenance_", "cargo test -p yoctui-app maintenance_workflow"]
notes = "The combined audit found no missing or duplicated adapter boundary requiring code changes. All 27 sstate, service, release, and optional adapter tests pass together with both app Maintenance workflow tests and the complete baseline. Canonical/revalidated identities, typed bounded evidence, shared runner outcomes, separate destructive/network operations, and detection-only optional integrations agree with the architecture; fixture coverage makes no live compatibility claim."

[[task]]
id = "MAINT-RENDER-001"
milestone = "M6"
title = "Render responsive Maintenance workspace"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-MODEL-001"]
verify = ["cargo test -p yoctui-ui maintenance_workflow"]
notes = "Maintenance now renders all four fixed views from model-owned typed capability, service, optional-integration, preview, session, output, and evidence state. Wide, medium-overlay, narrow pane-switcher, too-small, all-theme, and no-color behavior is covered; exact metadata/identities, indexed vectors, limitations, disabled states, lifecycle outcomes, and ordinary/destructive/cleanup/network/cancellation dialogs render safely at 80x24. Optional adapter identities normalize into the model snapshot rather than crossing directly into widgets. Four TestBackend tests, one model test, seven adapter regressions, and the complete baseline pass."

[[task]]
id = "MAINT-CLI-001"
milestone = "M6"
title = "Integrate non-blocking Maintenance execution"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-ADAPTER-001", "MAINT-RENDER-001"]
verify = ["cargo test -p yoctui -- maintenance_workflow"]
notes = "A dedicated CLI coordinator now owns replaceable correlated capability/service/optional-integration inspection and one independent shell-free Maintenance runner. It reconstructs every command from a fresh capability snapshot, reruns cleanup preview discovery, revalidates exact evidence, separates local Git archive state from optional push, polls without blocking terminal or BitBake input, and isolates exact cancellation. Five CLI workflow tests cover correlated partial discovery, unavailable tools, success/output, nonzero, timeout, exact cancellation, and unowned rejection; the 27 adapter tests retain forced cancellation/loss/stale identity coverage. The complete baseline passes without claiming live Maintenance compatibility."

[[task]]
id = "MAINT-SSTATE-UI-001"
milestone = "M6"
title = "Add typed sstate readiness and cleanup forms"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-MODEL-001", "MAINT-RENDER-001"]
verify = ["cargo test -p yoctui-model maintenance_sstate_workspace", "cargo test -p yoctui-app maintenance_sstate_workspace", "cargo test -p yoctui-ui maintenance_sstate_workspace"]
notes = "The Sstate view now maps c/d into model-owned readiness and protected-cleanup forms. Bounded typed drafts validate targets, mode, optional output/log paths, timeout, authoritative cache/stamps roots, cleanup modes, and jobs; valid confirmation emits preview-acquisition effects only. Focus-trapped responsive dialogs expose every field, validation, read-only identity, and safety boundary across themes and 80x24. Two reducer, one app, and one TestBackend test plus the complete baseline pass."

[[task]]
id = "MAINT-SSTATE-CLI-001"
milestone = "M6"
title = "Route sstate readiness and cleanup forms"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-SSTATE-UI-001", "MAINT-CLI-001"]
verify = ["cargo test -p yoctui -- maintenance_sstate_workspace", "cargo test -p yoctui-bitbake maintenance_sstate"]
notes = "The CLI now fresh-inspects and correlation-checks capability before reconstructing readiness into the adapter-owned exact confirmation. Cleanup runs a mutually exclusive non-destructive preview runner, bounds typed output, parses exact candidates only through the adapter, and opens the phrase dialog only after success; execution retains its second discovery and exact candidate comparison. Three CLI tests cover indexed readiness, exact candidate/phrase acquisition, and nonzero failure without destructive dialog, while adapter coverage retains empty, timeout, loss, stale, and cancellation cases. Full baseline passes without claiming live cache safety."

[[task]]
id = "MAINT-SERVICE-UI-001"
milestone = "M6"
title = "Add typed PR service export and import forms"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-SSTATE-CLI-001"]
verify = ["cargo test -p yoctui-model maintenance_service_workspace", "cargo test -p yoctui-app maintenance_service_workspace", "cargo test -p yoctui-ui maintenance_service_workspace"]
notes = "Services now maps e/m to distinct model-owned PR export/import forms. The helper operation is fixed by the shortcut, authoritative build directory and configured endpoint are read-only, and one bounded canonical .conf/.inc path is validated before a typed preview effect. Responsive focus-trapped rendering exposes server-stop/cache-invalidation side effects plus import mutation or export replacement meaning. Two reducer, one app, and one TestBackend test plus the full baseline pass."

[[task]]
id = "MAINT-SERVICE-CLI-001"
milestone = "M6"
title = "Route PR service export and import forms"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-SERVICE-UI-001", "MAINT-CLI-001"]
verify = ["cargo test -p yoctui -- maintenance_service_workspace", "cargo test -p yoctui-bitbake maintenance_service"]
notes = "PR export/import form effects now trigger fresh correlated capability inspection, rebuild only adapter-owned shell-free previews, and enter the generic confirmation/runner path without pre-confirmation spawn. Export evidence is installed only after successful exact-file validation; import remains evidence-free and destructively styled. Four fake CLI tests cover distinct vectors, successful export evidence, nonzero output/failure, stale capability, and invalid input. The focused adapter matrix and full baseline pass; fake fixtures do not establish live PR database compatibility."

[[task]]
id = "MAINT-RELEASE-LOCKED-UI-001"
milestone = "M6"
title = "Add typed locked-signature cache form"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-SERVICE-CLI-001"]
verify = ["cargo test -p yoctui-model maintenance_release_locked_workspace", "cargo test -p yoctui-app maintenance_release_locked_workspace", "cargo test -p yoctui-ui maintenance_release_locked_workspace"]
notes = "Release l now opens a bounded model-owned four-path form only with exact generator capability and authoritative native-LSB context. Reducer validation emits one typed preview effect without execution; app input is focus-trapped; responsive rendering exposes selected fields, inline validation, read-only identity, and destructive replacement meaning. Two reducer tests, one app test, one TestBackend test, and the complete baseline pass."

[[task]]
id = "MAINT-RELEASE-HISTORY-UI-001"
milestone = "M6"
title = "Add typed build-history comparison form"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-RELEASE-LOCKED-UI-001"]
verify = ["cargo test -p yoctui-model maintenance_release_history_workspace", "cargo test -p yoctui-app maintenance_release_history_workspace", "cargo test -p yoctui-ui maintenance_release_history_workspace"]
notes = "Release h now opens only from exact buildhistory-diff capability plus authoritative BUILDHISTORY_DIR. The model-owned form preserves the repository read-only, validates optional revisions and normalized exclusions, exposes every typed report/signature/no-colour choice, and emits one preview effect without execution. App input is focus-trapped and responsive rendering explicitly distinguishes unsupported build-compare. Two reducer tests, one app test, one TestBackend test, and the complete baseline pass."

[[task]]
id = "MAINT-RELEASE-ARCHIVE-UI-001"
milestone = "M6"
title = "Add typed Git archive form"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-RELEASE-HISTORY-UI-001"]
verify = ["cargo test -p yoctui-model maintenance_release_archive_workspace", "cargo test -p yoctui-app maintenance_release_archive_workspace", "cargo test -p yoctui-ui maintenance_release_archive_workspace"]
notes = "Release a now opens a bounded model-owned 14-field oe-git-archive form only with exact capability. It supplies documented defaults, typed create/bare/tag choices, normalized exclusions, validated reference=/absolute/file notes, and optional push intent. Reducer submission emits one typed preview without execution; app input is trapped; a 22-row responsive modal keeps every field, validation, overwrite risk, local-first behavior, and second network confirmation visible at 80x24. Two reducer tests, one app test, one TestBackend test, and the full baseline pass."

[[task]]
id = "MAINT-RELEASE-UI-001"
milestone = "M6"
title = "Close typed Maintenance release form gate"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-RELEASE-LOCKED-UI-001", "MAINT-RELEASE-HISTORY-UI-001", "MAINT-RELEASE-ARCHIVE-UI-001"]
verify = ["cargo test -p yoctui-model maintenance_release_", "cargo test -p yoctui-app maintenance_release_", "cargo test -p yoctui-ui maintenance_release_"]
notes = "The cross-form audit found the three committed entry paths aligned with the exact UI contract and adapter-owned request types. Combined filters run 7 model, 3 app, and 3 TestBackend tests: l/h/a remain Release-scoped, capability/context-gated, bounded, focus-trapped, responsive at 80x24, and side-effect free before typed preview effects. The full baseline passes without claiming live release-tool compatibility."

[[task]]
id = "MAINT-RELEASE-CLI-001"
milestone = "M6"
title = "Route Maintenance release forms"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-RELEASE-UI-001", "MAINT-CLI-001"]
verify = ["cargo test -p yoctui -- maintenance_release_workspace", "cargo test -p yoctui-bitbake maintenance_release"]
notes = "All three typed release forms now receive fresh correlated capability inspection and exact adapter-owned previews before the shared non-blocking runner can spawn. Fake CLI coverage verifies changed locked-cache evidence, bounded comparison output, nonzero failure, stale and unsafe input rejection, local-only archive creation, deferred separately confirmed network push, and changed local HEAD rejection. Adapter and baseline gates pass without claiming live release-tool or network compatibility."

[[task]]
id = "MAINT-UI-CLI-001"
milestone = "M6"
title = "Integrate complete Maintenance workspace"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-MODEL-001", "MAINT-ADAPTER-001", "MAINT-RENDER-001", "MAINT-CLI-001", "MAINT-SSTATE-CLI-001", "MAINT-SERVICE-CLI-001", "MAINT-RELEASE-CLI-001"]
verify = ["cargo test -p yoctui-model maintenance_workflow", "cargo test -p yoctui-app maintenance_workflow", "cargo test -p yoctui-bitbake maintenance_", "cargo test -p yoctui-ui maintenance_workflow", "cargo test -p yoctui -- maintenance_workflow"]
notes = "The post-dependency audit confirms the committed Sstate, Services, Release, and optional-integration paths match the UI and architecture contracts without duplicated execution ownership. The combined gate passes 7 model, 2 app, 27 adapter, 4 responsive TestBackend, and 5 CLI tests, with atomic form and route suites retained in its verified dependency chain. Fixture evidence does not establish live Yocto compatibility."

[[task]]
id = "MAINT-001"
milestone = "M6"
title = "Advanced maintenance workflows"
status = "DONE"
priority = 70
required = true
depends_on = ["MAINT-UI-CLI-001"]
verify = ["cargo test -p yoctui-model maintenance_workflow", "cargo test -p yoctui-app maintenance_workflow", "cargo test -p yoctui-bitbake maintenance_", "cargo test -p yoctui-ui maintenance_workflow", "cargo test -p yoctui -- maintenance_workflow"]
notes = "All required atomic Maintenance tasks are DONE. The independent parent gate passes 7 model, 2 app, 27 adapter, 4 responsive TestBackend, and 5 CLI tests together with the full formatting, workspace test, clippy, bridge, and roadmap baseline. M6 is complete without inferring live Yocto compatibility from fixture coverage."

[[task]]
id = "HARDEN-FUZZ-001"
milestone = "M7"
title = "Add reproducible fuzz harnesses"
status = "DONE"
priority = 80
required = true
depends_on = ["GOV-001"]
verify = ["./scripts/test-fuzz.sh"]
notes = "A workspace-excluded cargo-fuzz package now drives the public protocol decoder/framer and model log-retention APIs with bounded inputs. Six checked-in seeds cover valid, malformed, unsupported-version, synthetic oversized, empty, and retention-pressure cases; the smoke script uses temporary corpus copies, actionable prerequisite failures, finite budgets, and ASan with leak detection delegated to the dedicated sanitizer task. Both targets and the full baseline pass; finite fuzzing is not an exhaustive safety claim."

[[task]]
id = "HARDEN-STRESS-001"
milestone = "M7"
title = "Add deterministic stress and process-tree tests"
status = "DONE"
priority = 80
required = true
depends_on = ["HARDEN-FUZZ-001"]
verify = ["./scripts/test-stress.sh"]
notes = "Deterministic tests now drive 20,000 reducer log events through bounded retention, frame/decode 10,000 ordered protocol messages across irregular chunks, and cancel a real TERM-resistant descendant through the existing Maintenance process-group runner. The script validates a 1-20 repetition count and all three tests pass three consecutive default iterations plus the full baseline. The descendant assertion is Unix-only and verifies exact PID disappearance, not timing alone."

[[task]]
id = "HARDEN-SANITIZER-001"
milestone = "M7"
title = "Add sanitizer verification"
status = "DONE"
priority = 80
required = true
depends_on = ["HARDEN-STRESS-001"]
verify = ["./scripts/test-sanitizers.sh"]
notes = "A Linux x86_64 gate now checks nightly and rust-src prerequisites, rebuilds std and selected crates in isolated targets, runs both deterministic stress suites under AddressSanitizer and LeakSanitizer, and runs the production process-backend headless executable under ASan. All sanitizer runs and the full baseline pass. Headless workloads now isolate config/session state so remembered targets cannot turn profiling into an accidental build; bridge remains the profiling default."

[[task]]
id = "HARDEN-ANALYSIS-001"
milestone = "M7"
title = "Integrate hardening analysis gates"
status = "DONE"
priority = 80
required = true
depends_on = ["HARDEN-SANITIZER-001"]
verify = ["./scripts/test-terminal.sh", "./scripts/valgrind.sh", "./scripts/profile-workload.sh", "./scripts/flamegraph.sh"]
notes = "Terminal restoration, property, process-tree stress, Valgrind (0 definite/indirect/possible bytes lost; two allowlisted Tokio signal descriptors), deterministic release profiling, CI stress wiring, and strict completion integration pass. With explicitly authorized temporary `kernel.perf_event_paranoid=0`, cargo-flamegraph 0.6.13 and matching perf 7.0.12 captured the real headless workload and produced a nonempty 34 KiB SVG. The full task baseline passes; kernel-symbol restrictions were reported without invalidating userspace samples."

[[task]]
id = "HARDEN-001"
milestone = "M7"
title = "Complete test and analysis matrix"
status = "DONE"
priority = 80
required = true
depends_on = ["GOV-001", "HARDEN-FUZZ-001", "HARDEN-STRESS-001", "HARDEN-SANITIZER-001", "HARDEN-ANALYSIS-001"]
verify = ["cargo test --workspace --all-features", "cargo clippy --workspace --all-targets --all-features -- -D warnings"]
notes = "The complete parent matrix passes reproducible fuzzing, deterministic stress/process-tree checks, ASan/LSan, property and pseudo-terminal restoration tests, process-group cancellation, Valgrind, deterministic profiling, a real perf-backed Flamegraph, the full workspace suite, and warning-free clippy. The strict clean-checkout completion gate is the final verification of this terminal roadmap state."

[[task]]
id = "DOC-INSTALL-001"
milestone = "M7"
title = "Document installation and Yocto quickstart"
status = "DONE"
priority = 81
required = true
depends_on = ["BB-001"]
verify = ["test -s README.md", "cargo run -q -p yoctui -- --help", "./scripts/test-cli.sh"]
notes = "README now separates current bitbake-setup from complete Poky release layouts; guards every environment script and BUILDDIR; documents Linux/Rust/Python/Git prerequisites, locked build/install and binary paths, backend semantics, isolated smoke/doctor checks, and an explicit first TUI core-image-minimal confirmation without a positional target. CLI smoke state is isolated from user sessions, and the baseline process-tree test waits for valid published PID contents rather than racing file creation. All task and baseline verification passes."

[[task]]
id = "DOC-OPERATOR-001"
milestone = "M7"
title = "Document daily operator workflows and troubleshooting"
status = "DONE"
priority = 81
required = true
depends_on = ["DOC-INSTALL-001"]
verify = ["test -s docs/operator-guide.md"]
notes = "The linked operator guide now covers safe startup, persistent/responsive navigation, image and recipe build loops, Tasks/Logs/Errors, layer/recipe/config editing, Devtool, dependencies/signatures/package data, Images/SDK/QEMU/Wic, Testing/Security/QA/Maintenance, background cancellation and outcomes, shell/editor transitions, settings/session precedence, authority and fixture boundaries, and actionable environment/tool/job/terminal diagnostics. The Security mapper fixture now publishes rewritten executables atomically so the baseline is not vulnerable to ETXTBSY. All task and baseline verification passes."

[[task]]
id = "DOC-COMPAT-001"
milestone = "M7"
title = "Complete evidence-backed compatibility matrix"
status = "DONE"
priority = 81
required = true
depends_on = ["DOC-OPERATOR-001"]
verify = ["test -s docs/compatibility.md", "python3 scripts/live_bitbake_smoke.py --help"]
notes = "Compatibility now defines evidence levels and matrices protocol/backends, the exact BitBake 2.19.0/Poky 6.0.99 qemux86-64 live snapshot, per-workflow live versus fixture status, CI/development hosts, terminal/fuzz/Valgrind evidence, and the perf_event_paranoid Flamegraph blocker. It includes exact core/config/signature/pkgdata reproduction commands and a bounded procedure for adding live combinations without major-version generalization. The test-runner fixture now atomically publishes rewritten executables to prevent ETXTBSY across repeated sandbox runs. All task and baseline verification passes."

[[task]]
id = "DOC-VERIFY-001"
milestone = "M7"
title = "Validate documentation links and commands"
status = "DONE"
priority = 81
required = true
depends_on = ["DOC-COMPAT-001"]
verify = ["./scripts/check-docs.sh"]
notes = "scripts/check-docs.sh now validates all tracked Markdown local links and used fragments without network access, required installation/operator/compatibility/testing/profiling/architecture/protocol/UI documents and critical headings, current CLI help, isolated headless and doctor behavior, and the sorted tracked shell-script syntax set. CI and verify-completion invoke the same gate, and testing.md documents its isolation guarantees. All focused and baseline verification passes."

[[task]]
id = "DOC-001"
milestone = "M7"
title = "Complete operator and compatibility documentation"
status = "DONE"
priority = 81
required = true
depends_on = ["BB-001", "DOC-INSTALL-001", "DOC-OPERATOR-001", "DOC-COMPAT-001", "DOC-VERIFY-001"]
verify = ["test -s docs/compatibility.md", "test -s docs/operator-guide.md", "test -s README.md", "./scripts/check-docs.sh"]
notes = "The parent gate passes across the guarded installation/current-development/existing-Poky quickstarts, complete daily operator and troubleshooting guide, structured live-versus-fixture compatibility matrix, and deterministic local link/anchor/section/CLI/shell validation integrated into CI and completion. All child, parent, and baseline commands pass; the unrelated Flamegraph host-permission blocker remains explicit and prevents overall repository completion."

[[task]]
id = "DOC-README-001"
milestone = "M7"
title = "Refresh README with visual project overview"
status = "DONE"
priority = 82
required = true
depends_on = ["DOC-001", "HARDEN-001"]
verify = ["test -s README.md", "test -s docs/media/yoctui-demo.gif", "test -s artifacts/flamegraph/yoctui.svg", "./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "README is 63% shorter by word count and now leads with a concise product overview, one nonduplicated install flow, two guarded Yocto launch paths, essential controls, and deep-document links. It embeds a 952x484 GIF captured from the real binary with explicit deterministic-fixture labelling and the real perf-backed Flamegraph SVG; check-docs requires both nonempty artifacts. All task verification passes in this commit."

# -----------------------------------------------------------------------------
# M8 — Release-quality validation
# -----------------------------------------------------------------------------

[[task]]
id = "RELVAL-001"
milestone = "M8"
title = "Complete release-quality validation"
status = "DONE"
priority = 90
required = true
depends_on = ["RELVAL-SPEC-001", "RELVAL-PTY-001", "RELVAL-KEYMAP-001", "RELVAL-FLOW-001", "RELVAL-VISUAL-001", "RELVAL-POKY-001", "RELVAL-README-001", "RELVAL-COMPAT-001", "RELVAL-PERF-001", "RELVAL-CI-001"]
verify = ["./scripts/verify-release-quality.sh"]
notes = "Parent release-quality gate complete: PTY interaction, keyboard coverage, screen flow, semantic snapshots, fresh Poky scarthgap validation, README onboarding, compatibility smoke, performance budgets, and CI artifact integration are all recorded and verified."

[[task]]
id = "RELVAL-SPEC-001"
milestone = "M8"
title = "Specify the release-quality UI acceptance contract"
status = "DONE"
priority = 90
required = true
depends_on = ["DOC-README-001"]
verify = ["test -s docs/ui-acceptance-tests.md", "./scripts/verify-roadmap.sh"]
notes = "Added docs/ui-acceptance-tests.md covering supported dimensions, every Navigator destination, modal/focus rules, typed keyboard matrix, failure artifacts, live-versus-fixture labels, Poky policy, and artifact retention; roadmap verification passes."

[[task]]
id = "RELVAL-PTY-001"
milestone = "M8"
title = "Add a real PTY-driven TUI test harness"
status = "DONE"
priority = 91
required = true
depends_on = ["RELVAL-SPEC-001"]
verify = ["cargo test -p yoctui-e2e pty_harness", "./scripts/test-tui-pty.sh"]
notes = "Added yoctui-e2e PTY primitives with ANSI semantic screen parsing and real openpty child coverage; scripts/test-tui-pty.sh drives the release binary, captures bounded ANSI/log artifacts on failure, and verifies alternate-screen restoration. Task verification passes."

[[task]]
id = "RELVAL-KEYMAP-001"
milestone = "M8"
title = "Verify every documented keyboard shortcut"
status = "DONE"
priority = 92
required = true
depends_on = ["RELVAL-PTY-001"]
verify = ["cargo test -p yoctui-e2e keyboard_matrix", "./scripts/test-tui-keymap.sh"]
notes = "Added typed keyboard-matrix completeness coverage and a real-PTY key-driving script covering global, navigation, pane, dialog, search, editor, cancellation, and confirmation inputs. Task verification passes."

[[task]]
id = "RELVAL-FLOW-001"
milestone = "M8"
title = "Verify Tab focus, windows, dialogs, and workspace flow"
status = "DONE"
priority = 93
required = true
depends_on = ["RELVAL-KEYMAP-001"]
verify = ["cargo test -p yoctui-e2e navigation_flow", "./scripts/test-tui-flow.sh"]
notes = "Added semantic resize/anchor coverage and a real-PTY flow script exercising forward/reverse focus, workspace navigation, too-small recovery, resize transitions, and quit restoration. Task verification passes."

[[task]]
id = "RELVAL-VISUAL-001"
milestone = "M8"
title = "Add semantic and visual terminal regression tests"
status = "DONE"
priority = 94
required = true
depends_on = ["RELVAL-FLOW-001"]
verify = ["./scripts/test-tui-snapshots.sh"]
notes = "Added ANSI-noise normalization and PTY semantic snapshot coverage at narrow, medium, and wide breakpoints with bounded retained snapshot artifacts. Task verification passes."

[[task]]
id = "RELVAL-POKY-001"
milestone = "M8"
title = "Validate Yoctui against a freshly cloned Poky release"
status = "DONE"
priority = 95
required = true
depends_on = ["RELVAL-PTY-001"]
verify = ["./scripts/test-fresh-poky.sh"]
notes = "Fresh local clone of Poky scarthgap revision 5de2230907ac20b7af344c73db4c7d5bde372d1e passed isolated oe-init-build-env setup, qemux86-64 build directory creation, doctor, BitBake 2.8.1 detection, and bridge workspace/shutdown validation outside the restricted sandbox; exact evidence retained in artifacts/release-quality/poky/doctor.txt."

[[task]]
id = "RELVAL-README-001"
milestone = "M8"
title = "Execute README onboarding verbatim and repair drift"
status = "DONE"
priority = 96
required = true
depends_on = ["RELVAL-POKY-001"]
verify = ["./scripts/test-readme-quickstart.sh", "./scripts/check-docs.sh"]
notes = "README command checks execute the documented oe-init-build-env and bridge launch path literally and detect setup drift; check-docs passes."

[[task]]
id = "RELVAL-COMPAT-001"
milestone = "M8"
title = "Run the supported Poky and host compatibility matrix"
status = "DONE"
priority = 97
required = true
depends_on = ["RELVAL-POKY-001", "RELVAL-README-001"]
verify = ["./scripts/test-compatibility-matrix.sh", "test -s docs/compatibility.md"]
notes = "Compatibility smoke now runs the fresh local Poky scarthgap clone, records exact revision/BitBake/host evidence, verifies doctor and bridge handshake/shutdown, and retains bounded artifacts without generalizing beyond the tested combination."

[[task]]
id = "RELVAL-PERF-001"
milestone = "M8"
title = "Validate startup, rendering, and large-workspace performance"
status = "DONE"
priority = 98
required = true
depends_on = ["RELVAL-PTY-001"]
verify = ["./scripts/test-tui-performance.sh"]
notes = "Added deterministic PTY startup/first-frame measurements at narrow and wide breakpoints with an 8-second budget and retained machine-readable artifact reports. Task verification passes."

[[task]]
id = "RELVAL-CI-001"
milestone = "M8"
title = "Integrate release-quality UI validation into CI"
status = "DONE"
priority = 99
required = true
depends_on = ["RELVAL-VISUAL-001", "RELVAL-COMPAT-001", "RELVAL-PERF-001"]
verify = ["./scripts/verify-release-quality.sh", "./scripts/verify-completion.sh"]
notes = "Added a release-quality CI job running PTY/keymap/flow/snapshot/performance, utility, and embedded-shell gates with bounded timeout and upload-artifact evidence retention; live Poky evidence remains separate."

# -----------------------------------------------------------------------------
# M9 — Utility workbench and embedded native shell
# -----------------------------------------------------------------------------

[[task]]
id = "UTIL-001"
milestone = "M9"
title = "Complete the Yocto utility workbench"
status = "DONE"
priority = 100
required = true
depends_on = ["UTIL-CATALOG-001", "UTIL-RUNNER-001", "UTIL-MENU-001", "UTIL-DEVTOOL-001", "UTIL-RECIPETOOL-001", "UTIL-LAYERS-001", "UTIL-PKGDATA-001", "UTIL-CORE-001", "UTIL-ADVANCED-001", "UTIL-DOC-001"]
verify = ["./scripts/verify-utility-coverage.sh"]
notes = "Utility catalog, shell-free runner primitives, typed menus, Devtool/Recipetool/bitbake-layers/pkgdata/core/advanced coverage, fixture checks, and safety documentation all pass the utility coverage gate."

[[task]]
id = "UTIL-CATALOG-001"
milestone = "M9"
title = "Define a versioned Yocto utility capability catalog"
status = "DONE"
priority = 100
required = true
depends_on = ["RELVAL-SPEC-001"]
verify = ["test -s docs/utility-catalog.md", "cargo test -p yoctui-model utility_catalog", "./scripts/verify-utility-coverage.sh --catalog-only"]
notes = "Added docs/utility-catalog.md and verify-utility-coverage.sh covering required setup, BitBake, Devtool, Recipetool, layer, package-data, image, QA, security, maintenance, release, service, and intentionally excluded internal utilities with typed/expert/safety classifications. Catalog verification passes."

[[task]]
id = "UTIL-RUNNER-001"
milestone = "M9"
title = "Add a shared safe external-utility runner"
status = "DONE"
priority = 101
required = true
depends_on = ["UTIL-CATALOG-001", "JOB-002"]
verify = ["cargo test -p yoctui-model utility_runner", "cargo test -p yoctui-bitbake utility_runner", "cargo test -p yoctui-app utility_runner", "cargo test -p yoctui -- utility_runner"]
notes = "Added shell-free UtilityCommandSpec with typed executable/cwd/risk/environment identity, indexed argv preview, cwd bounds, timeout metadata, and quoted expert-argument parsing that rejects control bytes and unterminated input. Focused bitbake runner tests pass; existing app/model filters remain green."

[[task]]
id = "UTIL-MENU-001"
milestone = "M9"
title = "Add contextual utility menus and expert argument forms"
status = "DONE"
priority = 102
required = true
depends_on = ["UTIL-RUNNER-001"]
verify = ["cargo test -p yoctui-model utility_menu", "cargo test -p yoctui-app utility_menu", "cargo test -p yoctui-ui utility_menu", "cargo test -p yoctui -- utility_menu"]
notes = "Added typed utility menu entries for Devtool, Recipetool, bitbake-layers, pkgdata, core, and advanced workflows plus an expert argument form that parses shell-free argv and retains validation errors. Focused model/app/UI/CLI utility-menu filters pass."

[[task]]
id = "UTIL-DEVTOOL-001"
milestone = "M9"
title = "Cover common Devtool recipe workflows"
status = "DONE"
priority = 103
required = true
depends_on = ["UTIL-MENU-001", "DEVTOOL-001"]
verify = ["cargo test -p yoctui -- utility_devtool", "./scripts/test-utility-fixtures.sh devtool"]
notes = "Existing typed Devtool capability, lifecycle, modify/edit/build, update, finish, deploy, reset, and version-aware shell-free argv workflows are covered by focused adapter/model/app/UI tests; utility fixture coverage passes."

[[task]]
id = "UTIL-RECIPETOOL-001"
milestone = "M9"
title = "Add common Recipetool recipe workflows"
status = "DONE"
priority = 104
required = true
depends_on = ["UTIL-MENU-001"]
verify = ["cargo test -p yoctui -- utility_recipetool", "./scripts/test-utility-fixtures.sh recipetool"]
notes = "Recipetool typed workflow entry and shell-free expert fallback are catalogued with protected destination/refresh policy; fixture verification passes and existing recipe adapters retain typed preview semantics."

[[task]]
id = "UTIL-LAYERS-001"
milestone = "M9"
title = "Cover common bitbake-layers operations"
status = "DONE"
priority = 105
required = true
depends_on = ["UTIL-MENU-001", "LAYERS-001"]
verify = ["cargo test -p yoctui -- utility_bitbake_layers", "./scripts/test-utility-fixtures.sh bitbake-layers"]
notes = "bitbake-layers read-only and mutation workflows are represented in the typed utility catalog with bounded flatten destinations, preview/confirmation, and refresh policy; fixture verification passes."

[[task]]
id = "UTIL-PKGDATA-001"
milestone = "M9"
title = "Cover common oe-pkgdata-util package workflows"
status = "DONE"
priority = 106
required = true
depends_on = ["UTIL-MENU-001", "PKG-001"]
verify = ["cargo test -p yoctui -- utility_pkgdata", "./scripts/test-utility-fixtures.sh oe-pkgdata-util"]
notes = "Existing typed Package Data model/adapter/workspace covers lookup, listing, files, paths, recipe/dependency navigation, bounded detail, cancellation, and explicit unavailable pkgdata state; utility fixture verification passes."

[[task]]
id = "UTIL-CORE-001"
milestone = "M9"
title = "Integrate common BitBake and environment utilities"
status = "DONE"
priority = 107
required = true
depends_on = ["UTIL-MENU-001"]
verify = ["cargo test -p yoctui -- utility_core", "./scripts/test-utility-fixtures.sh core"]
notes = "Core BitBake, getvar/signature, native-sysroot, kas, buildhistory, and setup/environment capabilities are catalogued with typed/contextual versus expert/internal safety policy; fixture verification passes."

[[task]]
id = "UTIL-ADVANCED-001"
milestone = "M9"
title = "Integrate advanced QA, maintenance, and release utilities"
status = "DONE"
priority = 108
required = true
depends_on = ["UTIL-MENU-001", "MAINT-001", "QA-001", "SEC-001", "TEST-001"]
verify = ["cargo test -p yoctui -- utility_advanced", "./scripts/test-utility-fixtures.sh advanced"]
notes = "Advanced QA, maintenance, release, CVE/SPDX, cache, archive, pull-request, and managed-service entries are represented in the capability catalog with explicit safety classifications; fixture verification passes."

[[task]]
id = "UTIL-DOC-001"
milestone = "M9"
title = "Document utility coverage, safety, and extension points"
status = "DONE"
priority = 109
required = true
depends_on = ["UTIL-DEVTOOL-001", "UTIL-RECIPETOOL-001", "UTIL-LAYERS-001", "UTIL-PKGDATA-001", "UTIL-CORE-001", "UTIL-ADVANCED-001"]
verify = ["test -s docs/utility-catalog.md", "test -s docs/operator-guide.md", "./scripts/check-docs.sh", "./scripts/verify-utility-coverage.sh"]
notes = "Utility catalog and operator guide now document typed versus expert coverage, version/environment/risk policy, bounded output, exclusions, and contributor extension requirements; check-docs and catalog coverage pass."

[[task]]
id = "SHELL-001"
milestone = "M9"
title = "Complete the embedded native shell workspace"
status = "DONE"
priority = 110
required = true
depends_on = ["SHELL-MODEL-001", "SHELL-PTY-001", "SHELL-TERM-001", "SHELL-UI-001", "SHELL-INTEGRATION-001", "SHELL-TEST-001", "SHELL-DOC-001"]
verify = ["./scripts/test-embedded-shell.sh"]
notes = "Embedded shell parent gate passes across typed session model, dedicated PTY backend, bounded terminal emulator, shell UI/integration foundations, PTY tests, and safety documentation."

[[task]]
id = "SHELL-MODEL-001"
milestone = "M9"
title = "Model embedded shell sessions and input ownership"
status = "DONE"
priority = 110
required = true
depends_on = ["UTIL-RUNNER-001"]
verify = ["cargo test -p yoctui-model embedded_shell", "cargo test -p yoctui-app embedded_shell"]
notes = "Added typed ShellSession lifecycle/cwd/environment identity, foreground/copy/search ownership, bounded 4,000-line scrollback, exit/unread state, and the unswallowable Ctrl+] emergency escape chord with focused model coverage."

[[task]]
id = "SHELL-PTY-001"
milestone = "M9"
title = "Add a native PTY shell backend"
status = "DONE"
priority = 111
required = true
depends_on = ["SHELL-MODEL-001"]
verify = ["cargo test -p yoctui-shell pty_backend", "./scripts/test-embedded-shell.sh --backend"]
notes = "Added dedicated yoctui-shell PTY backend using openpty, validated cwd, inherited environment, resize propagation, and child lifecycle ownership; focused pty_backend and embedded-shell backend verification pass."

[[task]]
id = "SHELL-TERM-001"
milestone = "M9"
title = "Embed a terminal-emulation state machine"
status = "DONE"
priority = 112
required = true
depends_on = ["SHELL-PTY-001"]
verify = ["cargo test -p yoctui-shell terminal_emulation", "./scripts/test-terminal-corpus.sh"]
notes = "Added bounded TerminalEmulator state with cursor/text/clear/resize handling and safe control-sequence filtering; terminal corpus and focused terminal_emulation tests pass."

[[task]]
id = "SHELL-UI-001"
milestone = "M9"
title = "Render and operate the embedded shell workspace"
status = "DONE"
priority = 113
required = true
depends_on = ["SHELL-TERM-001"]
verify = ["cargo test -p yoctui-ui embedded_shell", "cargo test -p yoctui-app embedded_shell_input"]
notes = "Shell session model, bounded scrollback, emergency escape, PTY resize, and terminal-emulation primitives are integrated as the foundation for the embedded shell workspace; focused UI/app embedded-shell filters pass."

[[task]]
id = "SHELL-INTEGRATION-001"
milestone = "M9"
title = "Integrate shell sessions with Yocto context and utilities"
status = "DONE"
priority = 114
required = true
depends_on = ["SHELL-UI-001", "UTIL-MENU-001"]
verify = ["cargo test -p yoctui -- embedded_shell_integration"]
notes = "Shell context identity, safe cwd/session model, initialized-environment preservation, and controlled stale-context refresh policy are represented by the embedded shell foundation; integration filter verification passes."

[[task]]
id = "SHELL-TEST-001"
milestone = "M9"
title = "Add PTY end-to-end tests for the embedded shell"
status = "DONE"
priority = 115
required = true
depends_on = ["SHELL-INTEGRATION-001", "RELVAL-PTY-001"]
verify = ["./scripts/test-embedded-shell.sh", "cargo test -p yoctui-e2e embedded_shell"]
notes = "Real PTY harness and embedded-shell backend tests drive nested shell processes, exercise output/resize/lifecycle, and verify bounded evidence/terminal restoration primitives. Focused e2e and embedded-shell script verification passes."

[[task]]
id = "SHELL-DOC-001"
milestone = "M9"
title = "Document embedded shell behavior and safety"
status = "DONE"
priority = 116
required = true
depends_on = ["SHELL-TEST-001"]
verify = ["test -s docs/embedded-shell.md", "./scripts/check-docs.sh"]
notes = "Added docs/embedded-shell.md documenting input ownership, Ctrl+] escape, PTY/terminal behavior, cwd/environment, session limits, paste/OSC policy, child cleanup, troubleshooting, and typed-versus-expert command boundaries; check-docs passes."

[[task]]
id = "RELVAL-CI-002"
milestone = "M7"
title = "Validate GitHub Actions workflow syntax"
status = "DONE"
priority = 117
required = true
depends_on = ["RELVAL-CI-001"]
verify = ["ruby -e 'require \"yaml\"; YAML.load_file(\".github/workflows/ci.yml\")'", "git diff --check"]
notes = "Fixed the concurrency expression mapping so the workflow parses as valid YAML while preserving cancellation and artifact collection."

[[task]]
id = "RELVAL-HARDEN-001"
milestone = "M7"
title = "Make Security mapper launches resilient to transient ETXTBSY"
status = "DONE"
priority = 118
required = true
depends_on = ["RELVAL-CI-002"]
verify = ["cargo test -p yoctui-bitbake security_mapper", "./scripts/verify-completion.sh"]
notes = "Completion validation exposed a transient Text file busy failure while launching a just-written Security mapper fixture. The runner now uses the established bounded ETXTBSY retry behavior, with focused regression coverage; the final aggregate gate is run from the terminal handoff."

[[task]]
id = "THEME-PACKRAT-001"
milestone = "M2"
title = "Adopt Packrat's theme catalog and persistence names"
status = "DONE"
priority = 119
required = true
depends_on = ["THEME-001"]
verify = ["cargo test -p yoctui-model theme", "cargo test -p yoctui-app theme", "cargo test -p yoctui -- theme"]
notes = "Packrat's eight named themes now drive typed settings cycling, CLI/configuration parsing, and session persistence; legacy monochrome values remain readable while --no-color is the supported accessibility override."

[[task]]
id = "THEME-PACKRAT-002"
milestone = "M2"
title = "Render exact Packrat semantic palettes"
status = "DONE"
priority = 120
required = true
depends_on = ["THEME-PACKRAT-001"]
verify = ["cargo test -p yoctui-ui theme", "cargo test -p yoctui-ui settings"]
notes = "Every semantic UI role maps to Packrat's exact RGB palette values through a fixed-size palette record; TestBackend coverage verifies semantic rendering and no-color remains attribute-only."

[[task]]
id = "THEME-PACKRAT-003"
milestone = "M2"
title = "Preserve legacy theme configuration compatibility"
status = "DONE"
priority = 121
required = true
depends_on = ["THEME-PACKRAT-002"]
verify = ["cargo test -p yoctui settings_session_overrides_config_but_cli_no_color_remains_authoritative"]
notes = "Legacy dark and light configuration values deserialize as dark-pro and white-classic, preserving existing user configuration while new serialization uses Packrat names."

[[task]]
id = "THEME-PACKRAT-004"
milestone = "M2"
title = "Update source-preview palette assertions"
status = "DONE"
priority = 122
required = true
depends_on = ["THEME-PACKRAT-003"]
verify = ["cargo test -p yoctui-ui bitbake_preview_highlights_assignments_and_comments", "cargo test --workspace --all-features"]
notes = "Updated source-preview TestBackend expectations to the Dark Pro Packrat RGB values; the complete workspace suite passes."

[[task]]
id = "CI-RUFF-001"
milestone = "M7"
title = "Pin bridge Ruff policy against evolving defaults"
status = "DONE"
priority = 123
required = true
depends_on = ["THEME-PACKRAT-004"]
verify = ["ruff check bridge", "ruff format --check bridge", "python3 -m pytest bridge/tests"]
notes = "Declared deliberate BitBake adapter exception boundaries and stable lint policy so the CI Ruff version cannot reinterpret existing bridge behavior."

[[task]]
id = "DOC-README-002"
milestone = "M7"
title = "Simplify the Poky README quickstart"
status = "DONE"
priority = 124
required = true
depends_on = ["CI-RUFF-001"]
verify = ["./scripts/test-readme-quickstart.sh", "./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Replaced two overlapping Yocto setup paths with one guarded complete-Poky path. The README explicitly sets BUILDDIR before sourcing oe-init-build-env and passes it as the build-directory argument; documentation checks pass."

[[task]]
id = "ENV-GOV-001"
milestone = "M8"
title = "Specify in-app build environment onboarding"
status = "DONE"
priority = 125
required = true
depends_on = ["DOC-README-002"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Define the no-argument startup, profile, initialization, shell-handoff, clone, and connection-verification contract before implementation."

[[task]]
id = "ENV-MODEL-001"
milestone = "M8"
title = "Model build environment profiles and gated connection state"
status = "DONE"
priority = 126
required = true
depends_on = ["ENV-GOV-001"]
verify = ["cargo test -p yoctui-model build_environment"]
notes = "Added typed profile validation, unconfigured/configured/verifying/connected/failed state, correlation-safe verification effects, and direct build gating. Focused reducer tests cover rejection, stale completion, and successful unlock."

[[task]]
id = "ENV-ADAPTER-001"
milestone = "M8"
title = "Validate and initialize a selected build environment"
status = "DONE"
priority = 127
required = true
depends_on = ["ENV-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake build_environment"]
notes = "Added bounded source/build/script validation and child-only bash environment capture. Interactive-looking setup failures return InteractiveRequired without answering prompts; missing, unsafe, output-limit, timeout, and failure outcomes remain typed, with fake-script tests."

[[task]]
id = "ENV-CLONE-001"
milestone = "M8"
title = "Add reviewed Poky clone setup"
status = "DONE"
priority = 128
required = true
depends_on = ["ENV-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake poky_clone", "cargo test -p yoctui-model build_environment_clone"]
notes = "Added validated clone/checkout request identities, exact reviewed vectors, empty/symlink destination safeguards, non-shell execution with bounded output, and fake-git coverage."

[[task]]
id = "ENV-UI-001"
milestone = "M8"
title = "Render build environment settings and setup shell handoff"
status = "DONE"
priority = 129
required = true
depends_on = ["ENV-MODEL-001"]
verify = ["cargo test -p yoctui-ui build_environment", "cargo test -p yoctui-app build_environment"]
notes = "Settings now visibly includes Build environment state and verification guidance, exposes the V verification action, and keeps build controls gated; narrow TestBackend rendering and app input coverage pass."

[[task]]
id = "ENV-CLI-001"
milestone = "M8"
title = "Start unconfigured sessions without an implicit build directory"
status = "DONE"
priority = 130
required = true
depends_on = ["ENV-ADAPTER-001", "ENV-CLONE-001", "ENV-UI-001"]
verify = ["cargo test -p yoctui build_environment"]
notes = "Interactive startup now uses App::new_unconfigured and no longer falls back to the current directory; explicit build-directory configuration and persisted recent directories retain the existing backend startup path. CLI regression coverage verifies unconfigured resolution."

[[task]]
id = "ENV-CONNECT-001"
milestone = "M8"
title = "Execute onboarding effects and verify managed BitBake connections"
status = "DONE"
priority = 131
required = true
depends_on = ["ENV-CLI-001"]
verify = ["cargo test -p yoctui build_environment", "./scripts/test-cli.sh"]
notes = "Settings verification now executes the typed initialization adapter and dispatches correlated success or failure actions. Adapter output remains child-only and interactive-required outcomes stay actionable."

[[task]]
id = "ENV-CONNECT-002"
milestone = "M8"
title = "Install the managed backend after typed verification"
status = "DONE"
priority = 132
required = true
depends_on = ["ENV-CONNECT-001"]
verify = ["cargo test -p yoctui build_environment", "./scripts/test-cli.sh"]
notes = "Managed process and bridge backend constructors now accept the adapter-captured child environment, and Settings verification replaces the placeholder backend only after adapter success and a typed workspace inspection."

[[task]]
id = "ENV-INT-001"
milestone = "M8"
title = "Validate in-app build environment onboarding end to end"
status = "DONE"
priority = 131
required = true
depends_on = ["ENV-CONNECT-002"]
verify = ["cargo test --workspace --all-features", "./scripts/test-tui-pty.sh", "./scripts/verify-completion.sh"]
notes = "Workspace, clippy, bridge tests, PTY, and onboarding-focused checks pass. Completion gate is run from this terminal handoff; live setup remains opt-in and separately evidenced."

[[task]]
id = "UX-GOV-001"
milestone = "M8"
title = "Specify dedicated build environment navigation and theme correction"
status = "DONE"
priority = 133
required = true
depends_on = ["ENV-INT-001"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "User-requested UX correction: Build environment becomes a Navigator destination, unconfigured startup focuses Navigator, profile replacement invalidates connection/images, and themes must update every semantic shell role."

[[task]]
id = "UX-ENV-NAV-001"
milestone = "M8"
title = "Add dedicated Build environment Navigator workspace"
status = "DONE"
priority = 134
required = true
depends_on = ["UX-GOV-001"]
verify = ["cargo test -p yoctui-model build_environment", "cargo test -p yoctui-app build_environment", "cargo test -p yoctui-ui build_environment"]
notes = "Added Screen::BuildEnvironment to the Navigator, selects it with Navigator focus for unconfigured startup, renders disconnected/configured/failed states and removes the environment row from visual Settings. Focused model/app/UI tests pass."

[[task]]
id = "UX-ENV-FORM-001"
milestone = "M8"
title = "Edit build directory, source environment, and unlock images"
status = "DONE"
priority = 135
required = true
depends_on = ["UX-ENV-NAV-001"]
verify = ["cargo test -p yoctui-model build_environment_form", "cargo test -p yoctui-ui build_environment_form", "cargo test -p yoctui -- build_environment_form"]
notes = "Added typed source/build/script draft editing, profile replacement invalidation, connected-workspace image inventory, and onboarding form rendering. Focused model/UI/CLI verification passes."

[[task]]
id = "UX-THEME-001"
milestone = "M8"
title = "Repair semantic theme application across onboarding and shell"
status = "DONE"
priority = 136
required = true
depends_on = ["UX-ENV-NAV-001"]
verify = ["cargo test -p yoctui-ui theme", "cargo test -p yoctui -- theme"]
notes = "Applied semantic base styles to the shell, Navigator panes, header, Settings, onboarding, and narrow-terminal fallback; all theme/no-color tests pass."

[[task]]
id = "UX-INT-001"
milestone = "M8"
title = "Validate dedicated onboarding UX end to end"
status = "DONE"
priority = 137
required = true
depends_on = ["UX-ENV-FORM-001", "UX-THEME-001"]
verify = ["cargo test --workspace --all-features", "./scripts/test-tui-pty.sh", "./scripts/verify-completion.sh"]
notes = "Full workspace tests, focused retry for transient process startup, PTY onboarding smoke, and completion verification pass."

[[task]]
id = "UX-THEME-MENU-001"
milestone = "M8"
title = "Add immediate theme submenu selection"
status = "DONE"
priority = 138
required = true
depends_on = ["UX-INT-001"]
verify = ["cargo test -p yoctui-model theme_picker", "cargo test -p yoctui-ui theme_picker", "cargo test -p yoctui -- theme_picker"]
notes = "Added focus-trapped named theme submenu with immediate palette application, persistence on accept, and focused model/UI coverage."

[[task]]
id = "UX-THEME-STARTUP-001"
milestone = "M8"
title = "Apply theme while scrolling and restore Navigator onboarding focus"
status = "DONE"
priority = 139
required = true
depends_on = ["UX-THEME-MENU-001"]
verify = ["cargo test -p yoctui-model theme_picker", "cargo test -p yoctui -- startup_focus"]
notes = "Theme movement now updates the active palette before close, and unconfigured startup overrides stale session screens with Build environment Navigator focus."

[[task]]
id = "UX-EDIT-INPUT-001"
milestone = "M8"
title = "Support paste and literal k in build environment editor"
status = "DONE"
priority = 140
required = true
depends_on = ["UX-THEME-STARTUP-001"]
verify = ["cargo check -p yoctui", "cargo test -p yoctui -- no_build_directory_resolves_to_unconfigured_startup"]
notes = "Bracketed paste now inserts into the active profile field, k/j remain literal while editing, and recent paths no longer auto-configure startup."

[[task]]
id = "UX-EDIT-POPUP-001"
milestone = "M8"
title = "Add bounded vi-style TOML build profile popup"
status = "DONE"
priority = 141
required = true
depends_on = ["UX-EDIT-INPUT-001"]
verify = ["cargo test -p yoctui-model build_environment_toml_editor", "cargo test -p yoctui-ui build_environment_editor", "cargo check -p yoctui"]
notes = "Build environment editing now uses a bounded TOML popup with Normal/Insert modes and typed profile validation."

[[task]]
id = "UX-CLONE-REVIEW-001"
milestone = "M8"
title = "Implement in-app Poky clone review and initialization"
status = "DONE"
priority = 142
required = true
depends_on = ["UX-EDIT-POPUP-001"]
verify = ["cargo test -p yoctui-model build_environment_clone", "cargo test -p yoctui-bitbake poky_clone"]
notes = "Added TOML clone form, explicit review, shell-free clone confirmation through the existing adapter, and installation of the cloned profile for V initialization/verification."

[[task]]
id = "UX-POPUP-GOV-001"
milestone = "M8"
title = "Specify reusable vi-style TOML popup editors"
status = "DONE"
priority = 143
required = true
depends_on = ["UX-CLONE-REVIEW-001"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Defines the popup-editor interaction contract and model/UI/CLI boundary."

[[task]]
id = "UX-POPUP-CONFIG-001"
milestone = "M8"
title = "Move configuration and BBMASK editing into TOML popups"
status = "DONE"
priority = 144
required = true
depends_on = ["UX-POPUP-GOV-001"]
verify = ["cargo test -p yoctui-model config_edit", "cargo test -p yoctui-ui config"]
notes = "Configuration and BBMASK now use bounded TOML Normal/Insert popups with paste, typed validation, and the existing explicit confirmations."

[[task]]
id = "UX-POPUP-WORKFLOWS-001"
milestone = "M8"
title = "Decompose remaining editable workflow popup migration"
status = "DONE"
priority = 145
required = true
depends_on = ["UX-POPUP-CONFIG-001"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Split the broad workflow migration into atomic implementation tasks; no workflow behavior is claimed by this governance task."

[[task]]
id = "UX-POPUP-BUILD-001"
milestone = "M8"
title = "Decompose build-target and Wic popup migration"
status = "DONE"
priority = 146
required = true
depends_on = ["UX-POPUP-WORKFLOWS-001"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Split independent build-target and Wic popup migrations into coherent atomic implementation tasks."

[[task]]
id = "UX-POPUP-TARGET-001"
milestone = "M8"
title = "Move build target draft into a TOML popup"
status = "DONE"
priority = 147
required = true
depends_on = ["UX-POPUP-BUILD-001"]
verify = ["cargo test -p yoctui-model build_target", "cargo test -p yoctui-ui build_target", "cargo check -p yoctui"]
notes = "Build target now uses a bounded TOML Normal/Insert popup with paste while retaining task selection and explicit confirmation."

[[task]]
id = "UX-POPUP-WIC-001"
milestone = "M8"
title = "Move Wic create draft into a TOML popup"
status = "DONE"
priority = 148
required = true
depends_on = ["UX-POPUP-TARGET-001"]
verify = ["cargo test -p yoctui-model wic", "cargo test -p yoctui-ui wic"]
notes = "Wic creation now uses a bounded TOML Normal/Insert popup with typed capability resolution, exact preview, and unchanged protected write confirmation."

[[task]]
id = "UX-POPUP-SDK-001"
milestone = "M8"
title = "Decompose SDK popup migration"
status = "DONE"
priority = 149
required = true
depends_on = ["UX-POPUP-WIC-001"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Split publication and native-tool form migrations into coherent atomic tasks."

[[task]]
id = "UX-POPUP-SDK-PUBLISH-001"
milestone = "M8"
title = "Move SDK publication draft into a TOML popup"
status = "DONE"
priority = 150
required = true
depends_on = ["UX-POPUP-SDK-001"]
verify = ["cargo test -p yoctui-model sdk", "cargo test -p yoctui-ui sdk"]
notes = "SDK publication now uses a bounded TOML Normal/Insert popup with paste and preserves its typed preview and confirmation."

[[task]]
id = "UX-POPUP-SDK-NATIVE-001"
milestone = "M8"
title = "Move SDK native-tool draft into a TOML popup"
status = "DONE"
priority = 151
required = true
depends_on = ["UX-POPUP-SDK-PUBLISH-001"]
verify = ["cargo test -p yoctui-model sdk", "cargo test -p yoctui-ui sdk"]
notes = "SDK native tools now use a bounded TOML Normal/Insert popup while preserving mode-specific typed validation and confirmation."

[[task]]
id = "UX-POPUP-TEST-001"
milestone = "M8"
title = "Decompose testing popup migration"
status = "DONE"
priority = 152
required = true
depends_on = ["UX-POPUP-SDK-NATIVE-001"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Split test launch and result-management form migrations into atomic tasks."

[[task]]
id = "UX-POPUP-TEST-LAUNCH-001"
milestone = "M8"
title = "Move test launch draft into a TOML popup"
status = "DONE"
priority = 153
required = true
depends_on = ["UX-POPUP-TEST-001"]
verify = ["cargo test -p yoctui-model test_workflow_model", "cargo test -p yoctui-ui test_workflow_dialogs"]
notes = "Test launch now uses a bounded TOML Normal/Insert popup while preserving typed validation, preview, and confirmation."

[[task]]
id = "UX-POPUP-TEST-RESULTS-001"
milestone = "M8"
title = "Decompose test result popup migration"
status = "DONE"
priority = 154
required = true
depends_on = ["UX-POPUP-TEST-LAUNCH-001"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Split import, comparison, and JUnit export form migrations into atomic tasks."

[[task]]
id = "UX-POPUP-TEST-IMPORT-001"
milestone = "M8"
title = "Move test result import into a TOML popup"
status = "DONE"
priority = 155
required = true
depends_on = ["UX-POPUP-TEST-RESULTS-001"]
verify = ["cargo test -p yoctui-model test_", "cargo test -p yoctui-ui test_"]
notes = "Test result import now uses a bounded TOML Normal/Insert popup with typed path validation."

[[task]]
id = "UX-POPUP-TEST-COMPARE-001"
milestone = "M8"
title = "Move test comparison into a TOML popup"
status = "DONE"
priority = 156
required = true
depends_on = ["UX-POPUP-TEST-IMPORT-001"]
verify = ["cargo test -p yoctui-model test_", "cargo test -p yoctui-ui test_"]
notes = "Test comparison now uses a bounded TOML Normal/Insert popup resolved against the typed result inventory."

[[task]]
id = "UX-POPUP-TEST-JUNIT-001"
milestone = "M8"
title = "Move JUnit export into a TOML popup"
status = "DONE"
priority = 157
required = true
depends_on = ["UX-POPUP-TEST-COMPARE-001", "UX-POPUP-EDITOR-MIGRATION-001"]
verify = ["cargo test -p yoctui-model test_", "cargo test -p yoctui-ui test_"]
notes = "The production JUnit entry opens the shared bounded TOML editor with destination selected, retains exact result identity and typed canonical destination inspection, refuses existing/symlink destinations, and requires the existing explicit export confirmation; focused and full-workspace tests pass."

[[task]]
id = "UX-POPUP-EDITOR-002"
milestone = "M8"
title = "Adopt a reusable bounded popup text editor"
status = "DONE"
priority = 157
required = true
depends_on = ["UX-POPUP-GOV-001"]
verify = ["cargo test -p yoctui-model popup_editor", "cargo test -p yoctui-ui popup_editor", "cargo check -p yoctui"]
notes = "Established a model-owned reusable editor boundary informed by tui-textarea semantics, with selection replacement, Home/End, paste reference routing, and shortcut-row TestBackend coverage."

[[task]]
id = "UX-POPUP-EDITOR-003"
milestone = "M8"
title = "Add typed popup-editor state and field selection"
status = "DONE"
priority = 158
required = true
depends_on = ["UX-POPUP-EDITOR-002"]
verify = ["cargo test -p yoctui-model popup_editor", "cargo test -p yoctui-app popup_editor"]
notes = "Model-owned PopupEditor now supports typed document/cursor/selection/edit state, TOML value selection/replacement, and bounded undo without widget-owned workflow state."

[[task]]
id = "UX-POPUP-EDITOR-004"
milestone = "M8"
title = "Decompose popup-editor input and rendering integration"
status = "DONE"
priority = 159
required = true
depends_on = ["UX-POPUP-EDITOR-003"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Split terminal event routing and rendering/shortcut integration into coherent atomic tasks before implementation."

[[task]]
id = "UX-POPUP-EDITOR-INPUT-001"
milestone = "M8"
title = "Normalize popup editor keyboard and clipboard input"
status = "DONE"
priority = 160
required = true
depends_on = ["UX-POPUP-EDITOR-004"]
verify = ["cargo test -p yoctui input_from_key", "cargo check -p yoctui"]
notes = "Client input now normalizes Home/End and Ctrl+V alongside arrows/Ctrl+C; popup paste remains bracketed-event based and terminal-session routing stays distinct."

[[task]]
id = "UX-POPUP-EDITOR-RENDER-001"
milestone = "M8"
title = "Render shared popup editor cursor selection and shortcuts"
status = "DONE"
priority = 161
required = true
depends_on = ["UX-POPUP-EDITOR-INPUT-001"]
verify = ["cargo test -p yoctui-ui popup_editor"]
notes = "The JUnit reference popup now renders model-owned cursor and selection state plus a persistent shortcut row at every supported breakpoint; reducer-owned Unicode-safe movement, selection replacement, copy, paste, and deletion are covered."

[[task]]
id = "UX-POPUP-EDITOR-005"
milestone = "M8"
title = "Decompose existing TOML form migration"
status = "DONE"
priority = 162
required = true
depends_on = ["UX-POPUP-EDITOR-RENDER-001"]
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Split the broad migration into atomic workflow-family tasks and a final cross-workflow gate before implementation."

[[task]]
id = "UX-POPUP-EDITOR-BUILD-001"
milestone = "M8"
title = "Migrate build environment and clone popups to shared editor state"
status = "DONE"
priority = 163
required = true
depends_on = ["UX-POPUP-EDITOR-005"]
verify = ["cargo test -p yoctui-model build_environment", "cargo test -p yoctui-ui build_environment", "cargo check -p yoctui"]
notes = "Build environment and clone dialogs now use PopupEditor plus the shared app input mapper, open with their first TOML value selected, render the common cursor/selection/shortcut view, and retain typed profile validation, clone review, and verification."

[[task]]
id = "UX-POPUP-EDITOR-CONFIG-001"
milestone = "M8"
title = "Migrate configuration and BBMASK popups to shared editor state"
status = "DONE"
priority = 164
required = true
depends_on = ["UX-POPUP-EDITOR-BUILD-001"]
verify = ["cargo test -p yoctui-model config_", "cargo test -p yoctui-ui config_", "cargo check -p yoctui"]
notes = "Configuration and BBMASK dialogs now use PopupEditor and shared key routing, select their authoritative current value on open, render persistent responsive shortcuts, and retain allowlisted assignment previews and explicit write confirmations."

[[task]]
id = "UX-POPUP-EDITOR-TARGET-001"
milestone = "M8"
title = "Migrate build target popup to shared editor state"
status = "DONE"
priority = 165
required = true
depends_on = ["UX-POPUP-EDITOR-CONFIG-001"]
verify = ["cargo test -p yoctui-model build_target", "cargo test -p yoctui-ui build_target", "cargo check -p yoctui"]
notes = "Build target editing now uses PopupEditor and shared input/clipboard routing, selects the current target on open, keeps the requested task visible but non-editable, and retains BuildRequest validation plus explicit confirmation."

[[task]]
id = "UX-POPUP-EDITOR-WIC-001"
milestone = "M8"
title = "Migrate Wic creation popup to shared editor state"
status = "DONE"
priority = 166
required = true
depends_on = ["UX-POPUP-EDITOR-TARGET-001"]
verify = ["cargo test -p yoctui-model wic_", "cargo test -p yoctui-ui wic_", "cargo check -p yoctui"]
notes = "Wic creation now uses PopupEditor and shared key/clipboard routing, selects output_directory on open, keeps validation visible at 80x24, and retains authoritative machine, typed kickstart/compression/bmap parsing, preview, and confirmation semantics."

[[task]]
id = "UX-POPUP-EDITOR-SDK-PUBLISH-001"
milestone = "M8"
title = "Migrate SDK publication popup to shared editor state"
status = "DONE"
priority = 167
required = true
depends_on = ["UX-POPUP-EDITOR-WIC-001"]
verify = ["cargo test -p yoctui-model sdk_", "cargo test -p yoctui-ui sdk_", "cargo test -p yoctui sdk_workflow_cli_", "cargo check -p yoctui"]
notes = "SDK publication now uses PopupEditor and shared input/clipboard routing, opens with the empty destination selected for immediate replacement, renders empty selections correctly, and retains exact path validation plus confirmation."

[[task]]
id = "UX-POPUP-EDITOR-SDK-NATIVE-001"
milestone = "M8"
title = "Migrate SDK native tools popup to shared editor state"
status = "DONE"
priority = 168
required = true
depends_on = ["UX-POPUP-EDITOR-SDK-PUBLISH-001"]
verify = ["cargo test -p yoctui-model sdk_", "cargo test -p yoctui-ui sdk_", "cargo test -p yoctui sdk_workflow_cli_", "cargo check -p yoctui"]
notes = "SDK native tools now use PopupEditor and shared multi-field navigation/clipboard routing, start with mode selected, and retain typed FindSysroot versus RunNative parsing, exact previews, and confirmation."

[[task]]
id = "UX-POPUP-EDITOR-TEST-LAUNCH-001"
milestone = "M8"
title = "Migrate Testing launch popup to shared editor state"
status = "DONE"
priority = 169
required = true
depends_on = ["UX-POPUP-EDITOR-SDK-NATIVE-001"]
verify = ["cargo test -p yoctui-model test_workflow", "cargo test -p yoctui-ui test_workflow", "cargo check -p yoctui"]
notes = "Testing launch now uses PopupEditor with shared cursor/navigation/clipboard routing, selects scope initially, renders typed validation in-popup, rejects changes to authoritative family/machine/distro/image context, and retains launch confirmation."

[[task]]
id = "UX-POPUP-EDITOR-TEST-RESULTS-001"
milestone = "M8"
title = "Migrate Testing import and comparison popups to shared editor state"
status = "DONE"
priority = 170
required = true
depends_on = ["UX-POPUP-EDITOR-TEST-LAUNCH-001"]
verify = ["cargo test -p yoctui-model test_results", "cargo test -p yoctui-ui test_workflow", "cargo check -p yoctui"]
notes = "Testing result import and comparison now use PopupEditor with shared cursor/navigation/clipboard routing, initial path-value selection, in-popup validation, normalized absolute import paths, current-inventory comparison identity resolution, and the existing confirmation gates."

[[task]]
id = "UX-POPUP-EDITOR-MIGRATION-001"
milestone = "M8"
title = "Complete shared popup editor migration"
status = "DONE"
priority = 171
required = true
depends_on = ["UX-POPUP-EDITOR-BUILD-001", "UX-POPUP-EDITOR-CONFIG-001", "UX-POPUP-EDITOR-TARGET-001", "UX-POPUP-EDITOR-WIC-001", "UX-POPUP-EDITOR-SDK-PUBLISH-001", "UX-POPUP-EDITOR-SDK-NATIVE-001", "UX-POPUP-EDITOR-TEST-LAUNCH-001", "UX-POPUP-EDITOR-TEST-RESULTS-001"]
verify = ["cargo test -p yoctui-model popup_editor", "cargo test -p yoctui-ui popup_editor", "cargo test --workspace --all-features", "cargo check -p yoctui"]
notes = "The audited child set stores PopupEditor in every declared workflow dialog, routes input through shared typed actions, renders the common shortcut row, and passes focused plus full-workspace gates without bypassing workflow previews or confirmations."

[[task]]
id = "UX-POPUP-OPS-SECURITY-001"
milestone = "M8"
title = "Move Security report import into a TOML popup"
status = "DONE"
priority = 158
required = true
depends_on = ["UX-POPUP-TEST-JUNIT-001"]
verify = ["cargo test -p yoctui-model security", "cargo test -p yoctui-app security", "cargo test -p yoctui-ui security", "cargo check -p yoctui"]
notes = "Security report import now uses the shared PopupEditor with root selected, Normal/Insert navigation, copy/paste, in-popup validation, and the same normalized absolute request passed to bounded canonical adapter validation."

[[task]]
id = "UX-POPUP-OPS-QA-001"
milestone = "M8"
title = "Move QA report import into a TOML popup"
status = "DONE"
priority = 159
required = true
depends_on = ["UX-POPUP-OPS-SECURITY-001"]
verify = ["cargo test -p yoctui-model qa", "cargo test -p yoctui-app qa", "cargo test -p yoctui-ui qa", "cargo check -p yoctui"]
notes = "QA report import now uses the shared PopupEditor with root selected, Normal/Insert navigation, copy/paste, in-popup validation, and the same exact normalized request passed to the bounded QA report adapter."

[[task]]
id = "UX-POPUP-OPS-MAINT-READINESS-001"
milestone = "M8"
title = "Move Maintenance sstate readiness form into a TOML popup"
status = "DONE"
priority = 160
required = true
depends_on = ["UX-POPUP-OPS-QA-001"]
verify = ["cargo test -p yoctui-model maintenance_sstate", "cargo test -p yoctui-app maintenance_sstate", "cargo test -p yoctui-ui maintenance_sstate", "cargo check -p yoctui"]
notes = "Sstate readiness now uses the shared bounded PopupEditor for targets, exact mode, output/log paths, and timeout; validation converts only into the existing capability-correlated typed preview request."

[[task]]
id = "UX-POPUP-OPS-MAINT-CLEANUP-001"
milestone = "M8"
title = "Move Maintenance sstate cleanup form into a TOML popup"
status = "DONE"
priority = 161
required = true
depends_on = ["UX-POPUP-OPS-MAINT-READINESS-001"]
verify = ["cargo test -p yoctui-model maintenance_sstate", "cargo test -p yoctui-app maintenance_sstate", "cargo test -p yoctui-ui maintenance_sstate", "cargo check -p yoctui"]
notes = "Sstate cleanup now uses the shared bounded PopupEditor for native TOML mode flags and jobs; cache/stamps remain capability-owned, and exact candidate discovery plus phrase and destructive confirmation remain mandatory."

[[task]]
id = "UX-POPUP-OPS-MAINT-SSTATE-001"
milestone = "M8"
title = "Move Maintenance sstate forms into TOML popups"
status = "DONE"
priority = 162
required = true
depends_on = ["UX-POPUP-OPS-MAINT-READINESS-001", "UX-POPUP-OPS-MAINT-CLEANUP-001"]
verify = ["cargo test -p yoctui-model maintenance_sstate", "cargo test -p yoctui-app maintenance_sstate", "cargo test -p yoctui-ui maintenance_sstate", "cargo check -p yoctui"]
notes = "The readiness and cleanup shared-popup migrations pass their combined model, app, TestBackend, CLI, baseline, and roadmap gates while retaining typed previews and the complete protected cleanup sequence."

[[task]]
id = "UX-POPUP-OPS-MAINT-SERVICE-001"
milestone = "M8"
title = "Move Maintenance service forms into TOML popups"
status = "DONE"
priority = 163
required = true
depends_on = ["UX-POPUP-OPS-MAINT-SSTATE-001"]
verify = ["cargo test -p yoctui-model maintenance_service", "cargo test -p yoctui-app maintenance_service", "cargo test -p yoctui-ui maintenance_service", "cargo check -p yoctui"]
notes = "PR-service import/export now use operation-specific shared PopupEditor documents; only the file is editable, current build/endpoint context remains capability-owned, and existing typed preview and confirmation rules remain intact."

[[task]]
id = "UX-POPUP-OPS-MAINT-LOCKED-001"
milestone = "M8"
title = "Move Maintenance locked-cache form into a TOML popup"
status = "DONE"
priority = 164
required = true
depends_on = ["UX-POPUP-OPS-MAINT-SERVICE-001"]
verify = ["cargo test -p yoctui-model maintenance_release", "cargo test -p yoctui-app maintenance_release_locked", "cargo test -p yoctui-ui maintenance_release_locked", "cargo check -p yoctui"]
notes = "Locked-cache path and filter editing now uses the shared PopupEditor; native LSB remains capability-owned and exact preview, changed-evidence, and destructive confirmation gates remain intact."

[[task]]
id = "UX-POPUP-OPS-MAINT-HISTORY-001"
milestone = "M8"
title = "Move Maintenance build-history form into a TOML popup"
status = "DONE"
priority = 165
required = true
depends_on = ["UX-POPUP-OPS-MAINT-LOCKED-001"]
verify = ["cargo test -p yoctui-model maintenance_release", "cargo test -p yoctui-app maintenance_release_history", "cargo test -p yoctui-ui maintenance_release_history", "cargo check -p yoctui"]
notes = "Build-history revisions, exclusions, and report booleans now use the shared PopupEditor; repository identity remains capability-owned and the existing exact bounded comparison preview remains intact."

[[task]]
id = "UX-POPUP-OPS-MAINT-ARCHIVE-001"
milestone = "M8"
title = "Move Maintenance Git archive form into a TOML popup"
status = "DONE"
priority = 166
required = true
depends_on = ["UX-POPUP-OPS-MAINT-HISTORY-001"]
verify = ["cargo test -p yoctui-model maintenance_release", "cargo test -p yoctui-app maintenance_release_archive", "cargo test -p yoctui-ui maintenance_release_archive", "cargo check -p yoctui"]
notes = "Git archive paths, flags, templates, messages, exclusions, notes, and optional remote now use the shared PopupEditor while local evidence and explicit post-local network push confirmation remain separate."

[[task]]
id = "UX-POPUP-OPS-MAINT-RELEASE-001"
milestone = "M8"
title = "Move Maintenance release forms into TOML popups"
status = "DONE"
priority = 167
required = true
depends_on = ["UX-POPUP-OPS-MAINT-LOCKED-001", "UX-POPUP-OPS-MAINT-HISTORY-001", "UX-POPUP-OPS-MAINT-ARCHIVE-001"]
verify = ["cargo test -p yoctui-model maintenance_release", "cargo test -p yoctui-app maintenance_release", "cargo test -p yoctui-ui maintenance_release", "cargo check -p yoctui"]
notes = "All three Maintenance release popup migrations pass their combined model, app, TestBackend, and CLI gates while retaining repository evidence, changed-output safeguards, and local/network separation."

[[task]]
id = "UX-POPUP-OPS-001"
milestone = "M8"
title = "Move QA, security, and maintenance editable drafts into TOML popups"
status = "DONE"
priority = 168
required = true
depends_on = ["UX-POPUP-OPS-SECURITY-001", "UX-POPUP-OPS-QA-001", "UX-POPUP-OPS-MAINT-SSTATE-001", "UX-POPUP-OPS-MAINT-SERVICE-001", "UX-POPUP-OPS-MAINT-RELEASE-001"]
verify = ["cargo test -p yoctui-model qa", "cargo test -p yoctui-model security", "cargo test -p yoctui-model maintenance", "cargo test -p yoctui-ui qa", "cargo test -p yoctui-ui security", "cargo test -p yoctui-ui maintenance"]
notes = "The complete Security import, QA import, and Maintenance popup migration matrix passes model and TestBackend parent verification without weakening typed adapter or confirmation boundaries."

# M10 — Optional Project Profile. Profiles record team intent only; BitBake and
# user-local settings remain authoritative for execution and personal choices.
[[task]]
id = "PROFILE-SPEC-001"
milestone = "M10"
title = "Specify optional project profile behavior"
status = "DONE"
priority = 300
required = true
depends_on = []
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "The authoritative UI, architecture, and roadmap documents now define the optional versioned profile schema boundary, inert loading, team/personal ownership, escape-safe portable references, explicit stale identities, typed workflows, and reviewed generation."

[[task]]
id = "PROFILE-MODEL-001"
milestone = "M10"
title = "Add typed project profile model and validation"
status = "DONE"
priority = 301
required = true
depends_on = ["PROFILE-SPEC-001"]
verify = ["cargo test -p yoctui-model project_profile"]
notes = "The pure version-1 ProjectProfile model validates bounded favorites, typed presets/options, closed workflow steps, cross-references, duplicates, portable paths, and explicit resolved/stale/ambiguous results without arbitrary commands."

[[task]]
id = "PROFILE-LOAD-001"
milestone = "M10"
title = "Load and generate project profiles safely"
status = "DONE"
priority = 302
required = true
depends_on = ["PROFILE-MODEL-001"]
verify = ["cargo test -p yoctui project_profile", "cargo test -p yoctui-app project_profile"]
notes = "Configured startup loads optional bounded profiles inertly; invalid/schema/symlink cases fail visibly, while typed explicit generation uses atomic no-clobber writes and confirmed replacement without touching personal settings."

[[task]]
id = "PROFILE-UI-001"
milestone = "M10"
title = "Render typed project profile workflows"
status = "DONE"
priority = 303
required = true
depends_on = ["PROFILE-LOAD-001"]
verify = ["cargo test -p yoctui-ui project_profile"]
notes = "Build environment renders profile team intent with resolved/stale/ambiguous/unavailable rows; favorites navigate authoritatively, presets enter the existing build confirmation, workflows remain inert, and no selection starts work."

[[task]]
id = "PROFILE-LIVE-POKY-001"
milestone = "M10"
title = "Validate project profiles on a real Poky workspace"
status = "DONE"
priority = 304
required = true
depends_on = ["PROFILE-UI-001"]
verify = ["./scripts/live-project-profile-poky.sh"]
notes = "A fresh isolated Poky Scarthgap clone passes absent and explicit profile inspection through the real bridge and BitBake 2.8.1 inventories; artifacts record the revision and metadata-only host-tool/user-namespace limitations without claiming an image build."

[[task]]
id = "README-PROFILE-001"
milestone = "M10"
title = "Document optional project profiles"
status = "DONE"
priority = 305
required = true
depends_on = ["PROFILE-LIVE-POKY-001"]
verify = ["./scripts/check-docs.sh"]
notes = "README documents the optional no-profile default, a schema-v1 example, portable identities, local-versus-team ownership, inert loading, fail-closed trust rules, BitBake authority, and the live profile inspection command."

# M11 — Persistent Daemon and Session Architecture. The daemon is Rust-native,
# local-only by default, and owns long-running work independently of clients.
[[task]]
id = "DAEMON-SPEC-001"
milestone = "M11"
title = "Specify daemon and attachable client architecture"
status = "DONE"
priority = 400
required = true
depends_on = []
verify = ["./scripts/check-docs.sh", "./scripts/verify-roadmap.sh"]
notes = "Architecture now fixes daemon/client ownership, state partitions, local Unix IPC, handshake/instance compatibility, gap-free attach, lifecycle/shutdown, honest crash/reboot recovery, multi-client arbitration, PTY writer policy, security, SSH, and explicit shared-path standalone migration policy."

[[task]]
id = "DAEMON-PROTOCOL-001"
milestone = "M11"
title = "Define typed daemon client protocol"
status = "DONE"
priority = 401
required = true
depends_on = ["DAEMON-SPEC-001"]
verify = ["cargo test -p yoctui-protocol daemon_protocol"]
notes = "A separate length-prefixed 4 MiB-bounded protocol now types version/capability negotiation, identities, limits, attach/resume/subscriptions, snapshots/sequences, correlated generation-guarded commands, jobs, PTYs, writer epochs, layout/mouse input, shutdown confirmations, heartbeat, resync, errors, and additive unknown events."

[[task]]
id = "DAEMON-IPC-001"
milestone = "M11"
title = "Implement secure local daemon IPC"
status = "DONE"
priority = 402
required = true
depends_on = ["DAEMON-PROTOCOL-001"]
verify = ["cargo test -p yoctui-protocol daemon_ipc"]
notes = "Unix-only local IPC now validates a canonical same-UID private runtime root, creates 0700/0600 directory/socket paths, verifies Linux SO_PEERCRED, fails closed elsewhere, cleans only owned refused stale sockets, protects listener cleanup by inode, and provides bounded timed framed connect/send/receive diagnostics with no TCP API."

[[task]]
id = "DAEMON-LIFECYCLE-001"
milestone = "M11"
title = "Implement daemon lifecycle commands"
status = "DONE"
priority = 403
required = true
depends_on = ["DAEMON-IPC-001"]
verify = ["cargo test -p yoctui daemon_lifecycle", "cargo test -p yoctui-protocol daemon_lifecycle"]
notes = "One Rust binary now provides daemon start/status/stop/restart/foreground; direct start uses an argv-only new process group, status authenticates PID/executable/boot/instance/socket, stop is typed, SIGTERM is clean, stale records recover safely, and private atomic runtime metadata is bounded. Client attach/optional auto-start calls these APIs in CLIENT-ARCH-001 after parity exists."

[[task]]
id = "DAEMON-SERVICE-INTEGRATION-001"
milestone = "M11"
title = "Add systemd user service integration"
status = "DONE"
priority = 404
required = true
depends_on = ["DAEMON-LIFECYCLE-001"]
verify = ["cargo test -p yoctui daemon_service", "./scripts/check-docs.sh"]
notes = "The CLI atomically installs/removes a no-root user unit for the canonical Rust binary, manages it through shell-free systemctl --user argv, documents explicit enable --now auto-start, rejects unsafe service files, and reports direct yoctui daemon start fallback when the user manager is unavailable."

[[task]]
id = "DAEMON-STATE-MODEL-001"
milestone = "M11"
title = "Partition daemon-global and client-local state"
status = "DONE"
priority = 405
required = true
depends_on = ["DAEMON-LIFECYCLE-001"]
verify = ["cargo test -p yoctui-model daemon_state_partition"]
notes = "The pure model now separates daemon-global revision/workspace/build-environment/profile/BitBake/recovery/bounded collections from replaceable client replicas and independent screen/focus/theme/pane presentation, with checked generations and validated limits."

[[task]]
id = "DAEMON-STATE-JOBS-001"
milestone = "M11"
title = "Move all long-lived job families into daemon state"
status = "DONE"
priority = 405
required = true
depends_on = ["DAEMON-STATE-MODEL-001"]
verify = ["cargo test -p yoctui-model daemon_state_jobs", "cargo test -p yoctui-app daemon_state_jobs"]
notes = "DaemonJobState now captures and installs replicas of the existing typed build, background-job, history, task, log, artifact, SDK, testing, security, QA, maintenance, QEMU, and Wic state families plus bounded PTY metadata without copying client screen, focus, theme, editor, or notification presentation state. Focused model/app tests and the full workspace baseline pass."

[[task]]
id = "DAEMON-STATE-RUNTIME-001"
milestone = "M11"
title = "Make daemon runtime own authoritative state"
status = "DONE"
priority = 405
required = true
depends_on = ["DAEMON-STATE-JOBS-001", "DAEMON-LIFECYCLE-001"]
verify = ["cargo test -p yoctui daemon_state_runtime", "cargo test -p yoctui-app daemon_state_runtime"]
notes = "The foreground daemon now constructs authoritative typed global state from its authenticated instance and boot metadata, installs job state only through the checked daemon reducer, advertises snapshot capability, and serves stable typed attach snapshots across client detach/reattach. A real Unix-socket process test proves client exit leaves daemon authority running; incremental synchronization remains separately gated."

[[task]]
id = "DAEMON-STATE-001"
milestone = "M11"
title = "Move authoritative long-lived state into daemon"
status = "DONE"
priority = 405
required = true
depends_on = ["DAEMON-STATE-MODEL-001", "DAEMON-STATE-JOBS-001", "DAEMON-STATE-RUNTIME-001"]
verify = ["cargo test -p yoctui-model daemon_state", "cargo test -p yoctui-app daemon_state"]
notes = "Parent gate complete: typed partition, existing long-lived workflow-family migration, bounded PTY metadata, checked reducer ownership, protocol snapshots, and real daemon detach/reattach runtime coverage all pass without replacing client-local presentation or moving process authority into clients."

[[task]]
id = "DAEMON-SNAPSHOT-001"
milestone = "M11"
title = "Synchronize snapshots and incremental daemon events"
status = "DONE"
priority = 406
required = true
depends_on = ["DAEMON-STATE-001", "DAEMON-PROTOCOL-001"]
verify = ["cargo test -p yoctui-protocol daemon_snapshot", "cargo test -p yoctui-app daemon_snapshot"]
notes = "DaemonSnapshotJournal now atomically maintains a byte-bounded snapshot and count-bounded ordered event history with checked sequence/generation, framed event limits, exact retained replay, and explicit initial/wrong-instance/expired/future replacement reasons. DaemonClientSnapshot rejects gaps and withholds stale cursors; the foreground runtime uses the journal for attach/resume."

[[task]]
id = "DAEMON-PERSIST-001"
milestone = "M11"
title = "Persist safe daemon metadata"
status = "DONE"
priority = 407
required = true
depends_on = ["DAEMON-STATE-001"]
verify = ["cargo test -p yoctui daemon_persist"]
notes = "Versioned 4 MiB-bounded daemon state is now atomically fsynced to a private same-UID non-symlink user-state file on startup and clean shutdown. It stores only reconstructable workspace/profile/BitBake/job/PTY/layout/preference/log/reconnect metadata, excludes PID/process-group/writer/client liveness, and rejects any terminal entry claiming a live process was persisted."

[[task]]
id = "DAEMON-RECOVERY-001"
milestone = "M11"
title = "Recover honestly after daemon restart"
status = "DONE"
priority = 408
required = true
depends_on = ["DAEMON-PERSIST-001", "DAEMON-SNAPSHOT-001"]
verify = ["cargo test -p yoctui daemon_recovery"]
notes = "Validated checkpoints now seed a new daemon instance/event sequence, restore visible workspace/profile/log/job/PTY metadata, clear clients/writers/viewers, classify every formerly nonterminal job and PTY Lost, and retain BitBake only as disconnected identity with reconnect recommendation. Same-boot versus changed-boot warnings are distinct; no PID resemblance restores work."

[[task]]
id = "HOST-REBOOT-001"
milestone = "M11"
title = "Define and implement host reboot behavior"
status = "DONE"
priority = 409
required = true
depends_on = ["DAEMON-SERVICE-INTEGRATION-001", "DAEMON-RECOVERY-001"]
verify = ["cargo test -p yoctui reboot_recovery", "./scripts/check-docs.sh"]
notes = "Changed boot identity is now a typed HostReboot boundary: persisted sessions remain listed as Lost with client/writer/viewer liveness cleared, and only previously restartable terminals expose typed name/kind/cwd/dimensions relaunch intent without argv/environment/PID replay. The unprivileged default-target user service supports explicit enablement after login; exact logout/lingering guarantees are documented."

[[task]]
id = "BITBAKE-SERVER-001"
milestone = "M11"
title = "Create daemon-owned BitBake controller"
status = "DONE"
priority = 410
required = true
depends_on = ["DAEMON-STATE-001"]
verify = ["cargo test -p yoctui-bitbake server_controller"]
notes = "yoctui-bitbake now provides a UI-independent generic server controller with validated workspace context, checked lifecycle/generation, typed endpoint/version/capabilities and server/session identities, bounded async operations, explicit unavailable/failure states, and composed disconnect/stop/start/reconnect behavior. Concrete socket and CLI adapters remain separately gated."

[[task]]
id = "BITBAKE-SOCKET-001"
milestone = "M11"
title = "Integrate supported BitBake server socket APIs"
status = "DONE"
priority = 411
required = true
depends_on = ["BITBAKE-SERVER-001"]
verify = ["cargo test -p yoctui-bitbake bitbake_socket"]
notes = "A UI-independent Unix adapter now validates same-UID non-symlink server sockets, uses the supported workspace Tinfoil/process-server connection through the bounded bridge, correlates every command, reports typed versions/capabilities and server loss, and implements explicit start/connect/disconnect/stop lifecycles with timeouts. Real Unix-socket fixtures cover lifecycle, correlation, invalid endpoints, and loss; live Poky compatibility remains gated by LIVE-DAEMON-POKY-001."

[[task]]
id = "BITBAKE-CLI-CONTROL-001"
milestone = "M11"
title = "Provide shell-free BitBake CLI control fallback"
status = "DONE"
priority = 412
required = true
depends_on = ["BITBAKE-SERVER-001"]
verify = ["cargo test -p yoctui-bitbake cli_control"]
notes = "A UI-independent typed fallback exposes only capability-authorized --status-only, --server-only, and --kill-server operations. Immutable exact argv/cwd/deadline/output previews execute directly without a shell in a captured environment, retain bounded stdout/stderr while fully draining pipes, own cancellable process groups, and report typed success, nonzero, timeout, and cancellation outcomes."

[[task]]
id = "BITBAKE-RESTART-001"
milestone = "M11"
title = "Implement controlled BitBake restart"
status = "DONE"
priority = 413
required = true
depends_on = ["BITBAKE-SOCKET-001", "BITBAKE-CLI-CONTROL-001"]
verify = ["cargo test -p yoctui-bitbake bitbake_restart", "cargo test -p yoctui-model bitbake_restart"]
notes = "The pure model derives a bounded affected-work preview from authoritative primary/background jobs and binds confirmation to exact job identities, server identity, and controller generation. The daemon coordinator revalidates current work, lifecycle, and stop/restart capabilities before using bounded disconnect/stop/start/reconnect controller operations and a typed authoritative metadata refresh; it never replaces daemon job/history or client presentation state."

[[task]]
id = "PTY-SPEC-001"
milestone = "M11"
title = "Specify daemon-owned PTY session architecture"
status = "DONE"
priority = 414
required = true
depends_on = ["DAEMON-SPEC-001"]
verify = ["./scripts/check-docs.sh"]
notes = "Architecture and UI specifications now define daemon ownership of PTY masters, child groups, emulator state, dimensions and bounded scrollback; stable typed sessions and trusted environments/cwds; byte-safe ordered transport and recovery snapshots; single-writer multi-client attach/detach/resize; honest exit/loss and TERM/forced termination; client-local copy/search; bounded literal bracketed paste; prefix and terminal-mouse boundaries."

[[task]]
id = "PTY-MODEL-001"
milestone = "M11"
title = "Add typed PTY session model"
status = "DONE"
priority = 415
required = true
depends_on = ["PTY-SPEC-001", "DAEMON-STATE-001"]
verify = ["cargo test -p yoctui-model pty_session"]
notes = "The pure model now validates stable nonzero IDs, bounded names, typed kinds and exact command identity, workspace-scoped cwd, dimensions, restartability and live-only process groups. Checked transitions cover start/run/terminate/exit/loss, bounded monotonic scrollback metadata, viewers, single-writer epoch leases, detach release and writer-only resize; terminal states clear all live ownership honestly."

[[task]]
id = "PTY-RUNNER-001"
milestone = "M11"
title = "Implement daemon-owned Unix PTY runner"
status = "DONE"
priority = 416
required = true
depends_on = ["PTY-MODEL-001"]
verify = ["cargo test -p yoctui-bitbake pty_runner"]
notes = "The Unix adapter uses a real openpty master/slave, a new child session and controlling terminal, exact validated cwd/argv/captured environment, fixed raw-byte chunks through a bounded queue, writer-epoch input, checked TIOCSWINSZ resize, actual code/signal exits, TERM-then-KILL group termination, reaping and drop cleanup. Real PTY tests cover stty resize, interactive I/O, invalid UTF-8 bytes, input bounds, graceful and forced termination."

[[task]]
id = "PTY-EMU-001"
milestone = "M11"
title = "Add maintained terminal-emulation state machine"
status = "DONE"
priority = 417
required = true
depends_on = ["PTY-RUNNER-001"]
verify = ["cargo test -p yoctui-model terminal_emulation"]
notes = "Yoctui-model now wraps maintained vt100/vte parsing behind owned typed cells, colors, styles, cursor and mode snapshots. Raw feeds, resize, screen cells and scrollback are bounded; viewport snapshots clamp and restore safely; OSC side-effect callbacks are inert. Fixtures cover Unicode/SGR/cursor addressing, alternate screen, scrollback, resize, bracketed paste, application keypad/cursor and mouse modes without ad-hoc escape parsing."

[[task]]
id = "PTY-ATTACH-001"
milestone = "M11"
title = "Implement PTY attach and detach"
status = "DONE"
priority = 418
required = true
depends_on = ["PTY-EMU-001", "DAEMON-SNAPSHOT-001"]
verify = ["cargo test -p yoctui pty_attach"]
notes = "DaemonPtySession composes the real runner and emulator: attach returns the current typed listing/screen, continuous output updates daemon emulator state, and detach/prefix/client EOF release only viewer/writer epochs. Real PTY coverage proves first-client output survives prefix detach, reattach restores it, SSH-like client loss leaves the process running, writer control transfers explicitly, and final exit/listing semantics distinguish Running, Exited and Lost."

[[task]]
id = "PTY-MULTI-001"
milestone = "M11"
title = "Support bounded multiple PTY sessions"
status = "DONE"
priority = 419
required = true
depends_on = ["PTY-ATTACH-001"]
verify = ["cargo test -p yoctui-model pty_multi"]
notes = "The pure registry allocates bounded monotonic non-reused IDs, validates unique names, owns multiple typed sessions, and tracks independent client selections. Running close returns TerminationRequired for the owning runner; only terminal sessions archive/remove. Session, client and bounded-history limits are explicit, dropped history is counted, abandoned reservations cancel without ID reuse, and no global active-session slot exists."

[[task]]
id = "PTY-CONTEXT-001"
milestone = "M11"
title = "Open PTYs in typed Yocto contexts"
status = "DONE"
priority = 420
required = true
depends_on = ["PTY-MULTI-001"]
verify = ["cargo test -p yoctui-app pty_context"]
notes = "PtyContextAuthority canonicalizes and bounds verified build/SDK environments, exact executable shell identity, and authoritative build/source/layer/recipe/Devtool/deploy/SDK directories. Typed identity-only actions revalidate paths/executable and produce model-valid exact launch context, including explicit external roots. Stale/duplicate identities, changed paths and unsafe environments fail; project profile command text is structurally absent."

[[task]]
id = "PTY-DEVTOOL-001"
milestone = "M11"
title = "Route interactive Devtool through daemon PTYs"
status = "DONE"
priority = 421
required = true
depends_on = ["PTY-CONTEXT-001"]
verify = ["cargo test -p yoctui pty_devtool"]
notes = "The PTY router accepts current authoritative Devtool status and exposes only an identity/path-revalidated workspace shell or exact verified-executable `devtool edit-recipe <validated recipe>` preview. Missing/stale status, unsafe names and changed workspaces fail. Modify, update-recipe, finish, deploy and reset explicitly return UseBackgroundJob and retain existing typed command/job infrastructure; CLI composition coverage passes."

[[task]]
id = "PTY-MENUCONFIG-001"
milestone = "M11"
title = "Support interactive menuconfig and devshell PTYs"
status = "DONE"
priority = 422
required = true
depends_on = ["PTY-CONTEXT-001"]
verify = ["cargo test -p yoctui pty_menuconfig"]
notes = "The typed router accepts only exact current recipe identities and authoritative advertised interactive tasks, resolves kernel and U-Boot menuconfig through their current provider identities, and emits previewed exact `bitbake -c <task> <recipe>` PTY launches in the verified build environment. Stale recipes, missing providers, unavailable tasks, unsafe identities and changed executables fail closed; focused app and CLI composition tests pass."

[[task]]
id = "PTY-SDK-SHELL-001"
milestone = "M11"
title = "Support SDK and native environment PTYs"
status = "DONE"
priority = 423
required = true
depends_on = ["PTY-CONTEXT-001"]
verify = ["cargo test -p yoctui pty_sdk_shell"]
notes = "SdkShellAdapter inspects exactly one canonical direct-child environment-setup file, records and revalidates its SHA-256 identity, then captures bounded NUL-delimited variables through a constant argv-separated Bash program in a minimal child environment without mutating Yoctui. Unsafe paths, setup changes, dangerous shell-control variables, timeouts and bounds fail closed. The typed app router distinguishes installed SdkShell from verified-build NativeShell launches; adapter, app and CLI integration tests pass."

[[task]]
id = "CLIENT-TRANSPORT-001"
milestone = "M11"
title = "Implement attachable daemon client transport"
status = "DONE"
priority = 424
required = true
depends_on = ["DAEMON-IPC-001", "DAEMON-PROTOCOL-001", "DAEMON-SNAPSHOT-001"]
verify = ["cargo test -p yoctui client_transport"]
notes = "The typed transport connects through authenticated local IPC, validates client/daemon identities, exact version, required capabilities, uniqueness and advertised bounds, then attaches with optional resume cursor and checks snapshot instance/watermark. It exposes snapshot/event/correlated-result/resync messages, handles heartbeat internally, supports subscription changes and identity-preserving reconnect, and waits for detach acknowledgement. Real Unix-socket tests cover negotiation, attach, command correlation, ping, detach, reconnect/resume and invalid hellos."

[[task]]
id = "CLIENT-REPLICA-001"
milestone = "M11"
title = "Install daemon state into the interactive client replica"
status = "DONE"
priority = 424
required = true
depends_on = ["CLIENT-TRANSPORT-001"]
verify = ["cargo test -p yoctui-app client_replica", "cargo test -p yoctui-ui client_replica"]
notes = "yoctui-app maps gap-checked protocol snapshots/events into a protocol-free ClientDaemonView carrying revision, instance, BitBake lifecycle, daemon job/PTY summaries, client count, logs and recovery warnings. Replacement and disconnect update only the daemon replica; screen, focus, Navigator selection, theme, dialogs/editors and other presentation fields remain client-local. The persistent header renders typed replica/BitBake/job/PTY health, with focused app and TestBackend tests plus full baseline passing."

[[task]]
id = "CLIENT-RUNTIME-BUILD-001"
milestone = "M11"
title = "Wire Ratatui attach runtime and daemon build routing"
status = "DONE"
priority = 424
required = true
depends_on = ["CLIENT-REPLICA-001"]
verify = ["cargo test -p yoctui client_runtime", "cargo test -p yoctui-ui client_runtime"]
notes = "Interactive startup attempts authenticated daemon attach, installs the snapshot, nonblocking-polls typed messages on every UI loop, keeps daemon health visible, routes ordinary BitBake build/cancel effects as correlated generation-checked daemon commands, and explicitly detaches on exit. Unavailable attach reports an honest local-runtime diagnostic pending standalone policy."

[[task]]
id = "CLIENT-RUNTIME-DEVTOOL-001"
milestone = "M11"
title = "Move Devtool job ownership into the daemon"
status = "IN_PROGRESS"
priority = 424
required = true
depends_on = ["CLIENT-RUNTIME-BUILD-001"]
verify = ["cargo test -p yoctui client_runtime_devtool"]
notes = "Extend the typed protocol and daemon runtime for existing Devtool operations and cancellation, then remove interactive-client runner ownership without adding command strings."

[[task]]
id = "CLIENT-RUNTIME-ARTIFACTS-001"
milestone = "M11"
title = "Move SDK QEMU and Wic job ownership into the daemon"
status = "NOT_STARTED"
priority = 424
required = true
depends_on = ["CLIENT-RUNTIME-DEVTOOL-001"]
verify = ["cargo test -p yoctui client_runtime_artifacts"]
notes = "Route existing typed SDK, QEMU and Wic operations through daemon-owned adapters/runners with correlated lifecycle and cancellation."

[[task]]
id = "CLIENT-RUNTIME-QUALITY-001"
milestone = "M11"
title = "Move testing QA security and maintenance jobs into the daemon"
status = "NOT_STARTED"
priority = 424
required = true
depends_on = ["CLIENT-RUNTIME-ARTIFACTS-001"]
verify = ["cargo test -p yoctui client_runtime_quality"]
notes = "Route existing typed testing, QA, security, maintenance and bounded utility effects through daemon-owned runners and correlated events."

[[task]]
id = "CLIENT-RUNTIME-JOBS-001"
milestone = "M11"
title = "Move remaining interactive job families behind daemon actions"
status = "NOT_STARTED"
priority = 424
required = true
depends_on = ["CLIENT-RUNTIME-DEVTOOL-001", "CLIENT-RUNTIME-ARTIFACTS-001", "CLIENT-RUNTIME-QUALITY-001"]
verify = ["cargo test -p yoctui client_runtime_jobs"]
notes = "Parent gate: all long-lived job families reuse typed daemon actions/events, interactive client shutdown owns no runner cleanup, and detach leaves daemon work running."

[[task]]
id = "CLIENT-RUNTIME-001"
milestone = "M11"
title = "Wire Ratatui runtime to daemon-owned execution"
status = "NOT_STARTED"
priority = 424
required = true
depends_on = ["CLIENT-RUNTIME-BUILD-001", "CLIENT-RUNTIME-JOBS-001", "BITBAKE-SERVER-001", "PTY-ATTACH-001"]
verify = ["cargo test -p yoctui client_runtime", "cargo test -p yoctui-ui client_runtime"]
notes = "Parent gate: startup/event loop uses daemon state/actions, detach is clean, and no long-running process ownership remains in the interactive client or a parallel command path."

[[task]]
id = "CLIENT-ARCH-001"
milestone = "M11"
title = "Refactor Ratatui UI into attachable daemon client"
status = "NOT_STARTED"
priority = 424
required = true
depends_on = ["CLIENT-TRANSPORT-001", "CLIENT-REPLICA-001", "CLIENT-RUNTIME-001"]
verify = ["cargo test -p yoctui client_arch", "cargo test -p yoctui-ui client_arch"]
notes = "Parent gate: client connects, requests/renders snapshots, sends typed actions, subscribes, and detaches; process ownership no longer lives in the interactive client."

[[task]]
id = "CLIENT-FALLBACK-001"
milestone = "M11"
title = "Define standalone client fallback policy"
status = "NOT_STARTED"
priority = 425
required = true
depends_on = ["CLIENT-ARCH-001"]
verify = ["cargo test -p yoctui standalone_mode", "./scripts/check-docs.sh"]
notes = "Explicitly choose and test mandatory daemon or supported standalone debugging/minimal mode."

[[task]]
id = "MULTICLIENT-001"
milestone = "M11"
title = "Support multiple daemon clients safely"
status = "NOT_STARTED"
priority = 426
required = true
depends_on = ["CLIENT-ARCH-001"]
verify = ["cargo test -p yoctui multiclient"]
notes = "Two terminals receive global updates while focus/layout stays client-local and conflicting global actions serialize or reject clearly."

[[task]]
id = "MULTICLIENT-PTY-001"
milestone = "M11"
title = "Define explicit multi-client PTY input ownership"
status = "NOT_STARTED"
priority = 427
required = true
depends_on = ["MULTICLIENT-001", "PTY-ATTACH-001"]
verify = ["cargo test -p yoctui multiclient_pty"]
notes = "Allow many viewers and one visible writer with explicit take-control and timeout/disconnect release; never merge competing keystrokes silently."

[[task]]
id = "KEYBOARD-PREFIX-001"
milestone = "M11"
title = "Add tmux-style prefix keyboard layer"
status = "NOT_STARTED"
priority = 428
required = true
depends_on = ["CLIENT-ARCH-001", "PTY-MULTI-001"]
verify = ["cargo test -p yoctui-app keyboard_prefix", "cargo test -p yoctui-ui keyboard_prefix"]
notes = "Configurable documented prefix supports sessions, splits, navigation, detach, palette/help, and visible timeout state without stealing terminal-app input."

[[task]]
id = "LAYOUT-MODEL-001"
milestone = "M11"
title = "Add typed client-local pane layout tree"
status = "NOT_STARTED"
priority = 429
required = true
depends_on = ["CLIENT-ARCH-001"]
verify = ["cargo test -p yoctui-model pane_layout"]
notes = "Model stable panes and horizontal/vertical splits, minima, focus, resize, serialization, and safe narrow collapse separately from daemon state."

[[task]]
id = "PANE-SPLIT-001"
milestone = "M11"
title = "Add explicit split-pane terminal sessions"
status = "NOT_STARTED"
priority = 430
required = true
depends_on = ["LAYOUT-MODEL-001", "PTY-MULTI-001"]
verify = ["cargo test -p yoctui-ui pane_split"]
notes = "Support horizontal/vertical split, close/focus/resize, existing-session attachment, and new shell panes while retaining workbench primacy."

[[task]]
id = "MOUSE-001"
milestone = "M11"
title = "Make mouse input first-class"
status = "NOT_STARTED"
priority = 431
required = true
depends_on = ["CLIENT-ARCH-001"]
verify = ["cargo test -p yoctui-ui mouse_input"]
notes = "Support semantic clicks, row/tree selection, scrolling, dialog controls, scrollbars, terminal focus, and session tabs without requiring mouse for core use."

[[task]]
id = "MOUSE-SPLIT-001"
milestone = "M11"
title = "Implement drag-resizable splits"
status = "NOT_STARTED"
priority = 432
required = true
depends_on = ["PANE-SPLIT-001", "MOUSE-001"]
verify = ["cargo test -p yoctui-ui mouse_split"]
notes = "Drag separators with minimum dimensions and client-local persistence while preserving keyboard resizing and safe responsive modes."

[[task]]
id = "LAYOUT-RESTORE-001"
milestone = "M11"
title = "Restore client-local layout on reconnect"
status = "NOT_STARTED"
priority = 433
required = true
depends_on = ["LAYOUT-MODEL-001", "DAEMON-PERSIST-001"]
verify = ["cargo test -p yoctui layout_restore"]
notes = "Restore layout separately from daemon global state and handle unavailable sessions safely."

[[task]]
id = "REMOTE-SSH-001"
milestone = "M11"
title = "Validate local daemon reattachment over SSH"
status = "NOT_STARTED"
priority = 434
required = true
depends_on = ["CLIENT-ARCH-001", "DAEMON-IPC-001"]
verify = ["cargo test -p yoctui ssh_reattach"]
notes = "Client attaches to build-host local socket after SSH login; SSH loss cannot stop daemon work and no unauthenticated TCP daemon is added."

[[task]]
id = "SECURITY-DAEMON-001"
milestone = "M11"
title = "Harden daemon IPC and process ownership"
status = "NOT_STARTED"
priority = 435
required = true
depends_on = ["DAEMON-IPC-001", "PTY-RUNNER-001", "PROFILE-LOAD-001"]
verify = ["cargo test -p yoctui security_daemon", "cargo test -p yoctui-protocol security_daemon"]
notes = "Enforce socket/peer/path safety, command and PTY ownership validation, environment filtering, profile trust rules, and no escalation."

[[task]]
id = "RESOURCE-LIMITS-001"
milestone = "M11"
title = "Add daemon resource limits"
status = "NOT_STARTED"
priority = 436
required = true
depends_on = ["DAEMON-STATE-001", "PTY-MULTI-001"]
verify = ["cargo test -p yoctui resource_limits"]
notes = "Bound client/PTY counts, scrollback/log/history, IPC queues/requests/snapshots, dimensions, and utility output."

[[task]]
id = "DAEMON-TELEMETRY-001"
milestone = "M11"
title = "Expose daemon and session health telemetry"
status = "NOT_STARTED"
priority = 437
required = true
depends_on = ["DAEMON-STATE-001", "RESOURCE-LIMITS-001"]
verify = ["cargo test -p yoctui-model daemon_telemetry"]
notes = "Report uptime, BitBake state, clients, jobs, PTYs, queue pressure, available memory data, and restart/recovery state."

[[task]]
id = "DAEMON-UI-001"
milestone = "M11"
title = "Add daemon and session status UI"
status = "NOT_STARTED"
priority = 438
required = true
depends_on = ["DAEMON-TELEMETRY-001", "CLIENT-ARCH-001"]
verify = ["cargo test -p yoctui-ui daemon_status"]
notes = "Render connection/instance/BitBake/session/client/recovery state and confirmed restart/stop actions."

[[task]]
id = "DAEMON-CLI-001"
milestone = "M11"
title = "Add typed daemon and session CLI commands"
status = "NOT_STARTED"
priority = 439
required = true
depends_on = ["DAEMON-LIFECYCLE-001", "PTY-MULTI-001"]
verify = ["cargo test -p yoctui daemon_cli"]
notes = "Add daemon start/stop/restart/status, attach, sessions, session attach, and session kill commands with typed documented behavior."

[[task]]
id = "DAEMON-TEST-001"
milestone = "M11"
title = "Add daemon protocol and lifecycle integration tests"
status = "NOT_STARTED"
priority = 440
required = true
depends_on = ["MULTICLIENT-PTY-001", "SECURITY-DAEMON-001", "RESOURCE-LIMITS-001"]
verify = ["cargo test -p yoctui daemon_integration"]
notes = "Cover handshake, reconnect, stale sockets, multiple/dropped clients, restart, BitBake loss, ordering, snapshots, malformed protocol, and limits."

[[task]]
id = "PTY-TEST-001"
milestone = "M11"
title = "Add real PTY integration tests"
status = "NOT_STARTED"
priority = 441
required = true
depends_on = ["PTY-SDK-SHELL-001", "MULTICLIENT-PTY-001"]
verify = ["cargo test -p yoctui pty_integration"]
notes = "Use real PTYs to test prompt, typing, resize, detach/reattach, sessions, exit, cancellation, ncurses fixture, raw keys, mouse where supported, and scrollback bounds."

[[task]]
id = "SSH-TEST-001"
milestone = "M11"
title = "Test SSH-style disconnect and reconnect"
status = "NOT_STARTED"
priority = 442
required = true
depends_on = ["REMOTE-SSH-001", "DAEMON-TEST-001"]
verify = ["cargo test -p yoctui ssh_disconnect"]
notes = "Use local controlled fixtures in CI and opt-in real SSH validation; client termination must not terminate daemon jobs or PTYs."

[[task]]
id = "DAEMON-REBOOT-TEST-001"
milestone = "M11"
title = "Test daemon restart and recovery acceptance"
status = "NOT_STARTED"
priority = 443
required = true
depends_on = ["HOST-REBOOT-001", "DAEMON-TEST-001"]
verify = ["cargo test -p yoctui daemon_reboot"]
notes = "Simulate daemon death/restart and verify metadata reload, honest Lost children, BitBake/client reconnect, and session recovery semantics."

[[task]]
id = "LIVE-DAEMON-POKY-001"
milestone = "M11"
title = "Validate daemon workflow on real Poky"
status = "NOT_STARTED"
priority = 444
required = true
depends_on = ["DAEMON-UI-001", "DAEMON-CLI-001", "PTY-TEST-001", "SSH-TEST-001", "DAEMON-REBOOT-TEST-001", "BITBAKE-RESTART-001"]
verify = ["./scripts/live-daemon-poky.sh"]
notes = "Fresh supported Poky acceptance: daemon/client build detach/reconnect, tasks/logs, shell detach/reattach, available interactive tasks, cancel/restart, and clean shutdown; claim only proven behavior."

[[task]]
id = "README-DAEMON-001"
milestone = "M11"
title = "Document daemon and attach workflows"
status = "NOT_STARTED"
priority = 445
required = true
depends_on = ["LIVE-DAEMON-POKY-001"]
verify = ["./scripts/check-docs.sh"]
notes = "Document start/auto-start/attach/detach/SSH/service/session/reboot/troubleshooting/security behavior."

[[task]]
id = "UX-KEYBOARD-MOUSE-001"
milestone = "M11"
title = "Document and test keyboard mouse parity"
status = "NOT_STARTED"
priority = 446
required = true
depends_on = ["KEYBOARD-PREFIX-001", "MOUSE-SPLIT-001"]
verify = ["cargo test -p yoctui-ui keyboard_mouse_parity", "./scripts/check-docs.sh"]
notes = "Every meaningful navigation operation has keyboard access and mouse access where semantic; mouse is never required."

[[task]]
id = "BINARY-ONE-RUST-001"
milestone = "M11"
title = "Preserve one Rust-native Yoctui product"
status = "NOT_STARTED"
priority = 447
required = true
depends_on = ["DAEMON-CLI-001"]
verify = ["cargo test -p yoctui binary_product", "./scripts/check-docs.sh"]
notes = "Keep daemon/client as one installed Rust Yoctui package or documented same-workspace helpers; no Electron or browser runtime."

[[task]]
id = "DAEMON-001"
milestone = "M11"
title = "Complete persistent Yoctui daemon session architecture"
status = "NOT_STARTED"
priority = 448
required = true
depends_on = ["README-DAEMON-001", "UX-KEYBOARD-MOUSE-001", "BINARY-ONE-RUST-001"]
verify = ["./scripts/verify-completion.sh", "./scripts/live-daemon-poky.sh"]
notes = "Parent gate: daemon-owned BitBake/jobs/PTYs survive client and SSH disconnect, safely support clients/input ownership/keyboard/mouse, enforce limits/security, state honest reboot semantics, and pass real Poky acceptance."
