# v0.1.64 release validation

Candidate: source checkpoint `e7378022fa8a9369ebb959df5dfc1425ec0fd88c`
plus the version/dependency changes in
[the build-source patch](../../performance/real-poky/v0.1.64-source.patch).
Application runtime source is unchanged from that checkpoint. The compatible
`lru` update to 0.18.4 fixes RUSTSEC-2026-0253; no advisory ignore was added.

The user approved only the reviewed fixed tui-logger/Jiff cache exception.
[Fresh Memcheck evidence](0.1.64-memcheck.json) records the exact 39,367 bytes
in ten allocations, no growth over 16/128 frames, and zero definite/indirect
losses. All other findings remain fatal. Ten policy tests include unknown-stack,
size/count/growth, dependency, incomplete-report and unrelated-error controls.

## Rendering measurements

The unchanged 10 ms/frame gate was rerun after compiler processes exited.
At 160x48, 300 frames per workbench scenario:

| Scenario | ms/frame |
| --- | ---: |
| Menus | 0.746 |
| Large rootfs | 1.694 |
| Large graph | 6.553 |
| Large editor | 8.308 |
| Dense terminal | 1.215 |

The five-scenario 500-frame next-generation UI matrix also passed.
An earlier run while Rust's optimized linker was active failed the large-editor
limit at 13.030 ms/frame. That result is not a passing baseline. Its local log
is `/tmp/yoctui-v64-workbench-performance.log`; the quiet rerun is
`/tmp/yoctui-v64-workbench-performance-quiet.log`. No threshold was changed.

The patched-dependency flamegraph captured 2,796 samples over 6,000 frames with
zero unresolved frames. Its tracked SVG and summary are under
`artifacts/flamegraph/`; later completion runs refresh those artifacts.

## CPU evidence

Fresh idle daemon plus one attached client: 0.1872% of one logical CPU, using
the existing ten-second warmup and sixty one-second samples per scenario.
The source-bound records are under `artifacts/performance/results/low-overhead/`.
Fresh Poky 6.0.2 kernel compilation at 99.8393% host CPU over 360 seconds:
daemon plus one attached client consumed 0.9333% of one logical CPU.
Keyboard-to-frame p95 was 4.6814 ms, cancellation acknowledgement 4.8349 ms,
and fresh attach 21.9319 ms. Backend continuity and critical-event delivery
passed. The source-bound raw samples and manifest are under
`artifacts/performance/real-poky/`. Only the private fixture was cleaned/built;
the user's normal build and daemon were not changed.

The full `./scripts/verify-completion.sh` gate passed with exit status 0 at
candidate commit `5c91f97a73d3e4587963cfd16bc78473e760a3ca`. Final evidence/docs
are folded into the same unpushed release commit; application sources and
dependency versions are unchanged from the tested candidate.
Local full log: `/tmp/yoctui-v64-completion.log`, SHA-256
`b746a680c0f4e8c6cb284c0c9266c474dfe9d11438c56e43cb31e5931f756dc5`.

Coverage: model 86.27%, protocol 89.72%, bridge 76.01%, all above their unchanged
80%/80%/75% gates. Audit/deny, workspace/Clippy, terminal, fuzz, repeated stress,
ASan/LSan and final Memcheck passed. The final flamegraph contains 2,403 samples
over 6,000 frames with zero unresolved frames. The optional standalone live
BitBake smoke explicitly skipped because its opt-in environment was unset;
the independent mandatory real-Poky performance evidence passed separately.

## Publication

All six public archives passed isolation/bridge checks and Cargo's batch
publication dry run. Full completion passed; registry upload and fresh-install
verification are the remaining delivery steps. No upload has occurred yet.
